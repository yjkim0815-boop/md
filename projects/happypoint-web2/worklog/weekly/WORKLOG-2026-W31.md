---
문서유형: WORKLOG
범위: 프로젝트(happypoint-web2)
주기: 주
기간: 2026-W31 (07-27~)
이슈키: WORK-16611
작성일: 2026-07-27
최종수정: 2026-07-29
작성자: dominic
상태: 진행중
요약: KCB 본인인증(휴대폰·아이핀) React 연동, SSR 로그인 판별, 서버 파일 로그, /api 라우팅 통일 (+07-29: 로그인체크 최소화·dev 도메인·로컬로그인 BFF프록시 일원화)
---

# 🛠️ happypoint-web2 주간 작업내역 — 2026-W31

## 0. (08-06) feature/WORK-16613 → main 병합 (merge 59c90ef)
- **범위**: 이번 세션 작업 전체(파바앱 회원가입/아이디·비번찾기/휴면 흐름·크롬숨김, 일반 휴면 신규, Airbridge 전역 SDK+트래킹링크, 환경별 build 스크립트). 75파일 +1559/-483.
- **충돌 1곳**: `app/(site)/page/join/welcome/page.tsx` — main 리디자인 커밋(3D 배지·QR·PC/MO 버튼)과 feature의 airbridge 버튼 교체가 같은 파일. **해결**: main 리디자인 유지 + 앱 CTA만 `AppOpenCta`→`AirbridgeSignupDeeplink`.
- **lockfile 드리프트 수정**: main·feature 둘 다 pnpm-lock 수정 → 텍스트 자동병합이 깨짐(frozen 배포 위험). 확인 결과 **병합 package.json 의존성 = main 과 동일**(feature 는 build 스크립트만 추가, 신규 dep 없음) → `git checkout main -- pnpm-lock.yaml` 로 main lockfile 채택 → package.json↔lock 정합 복구.
- **⚠️ 로컬 검증 한계**: 로컬 pnpm/corepack 셔임 손상 + node_modules 프로세스 잠금으로 `pnpm install`/`tsc`/`build` **로컬 미실행**. tsc 에서 보인 에러는 (a)`.next` stale 캐시(리네임 이전 브랜드 find 경로 — 재빌드 시 재생성) (b)`three` 미설치(main 리디자인 dep, lockfile엔 존재 → Jenkins 빌드 시 설치)로 **병합 결함 아님**. object-3d.tsx 는 main 원본 그대로. **→ Jenkins dev/stg 빌드(`build:dev`/`build:stg`)로 최종 타입/빌드 확인 필요. 푸시는 미실행(사용자 확인 후).**

## 0-1. (08-06) /page/event 3라우트 + 연결 모델API·인프라 삭제 (⏳배포대기, 프론트+백엔드 커플링)
사용자 지시로 이벤트 3개 화면 및 연결 자원 전량 삭제. **프론트(happypoint-web2)**:
- 라우트 삭제: `app/(site)/page/event/{event-list(page+event-browser), my-coupon(page+my-coupon-browser), coupon-info(page)}`
- 고아 정리: `components/layout/event-tabs.tsx`(EventTabs, 3화면 전용 nav) · BFF `app/api/event/{list,my-coupon}/route.ts` · lib `legacy-event.ts`·`legacy-my-coupon.ts` · interface `page/event/{event-list,coupon-info}.ts`
- 참조 정리: `app/sitemap.ts`(event-list·coupon-info 엔트리 제거) · `lib/search-index.ts`("이벤트·쿠폰" 항목 제거)
- 유지: `category-tabs.tsx`(alliance 등 공용) · `home-data.ts` couponFaq(미사용 데이터로 남음, 무해) · 이벤트 나머지(event-view/winner-*/ai-jukebox/event-proc) 라우트·인터페이스

**백엔드([ha-web-api] j-ha-web-api)**: `EventModelApiResource.java` 에서 **@GetMapping 3개 제거** — `/api/event/event-list`·`/api/event/coupon-info`·`/api/event/my-coupon`(+클래스 계약 주석 정합). 나머지 엔드포인트(event-view/winner-list/winner-view/ai-jukebox/event-proc) 유지. ⚠️ 제거로 `eventCouponIosInfoUrl`/`AosInfoUrl` @Value 필드·일부 import 미사용(경고만, 빌드 무해). **⚠️ 프론트+백엔드 함께 배포**(한쪽만 배포 시 404 라우트/데드 엔드포인트). **→ 배포 후 /page/event/* 404 + 남은 이벤트 기능 정상 확인.**

## 0-2. (08-06, ⏳배포대기) 파바앱(OPBS) 상단 헤더 여백 절반 축소
사용자 지시 — 파바앱은 헤더 미렌더하되 그 자리 공백을 유지했는데, 그 **헤더 자리 여백을 절반으로**. 헤더 여백 = `components/ui/subpage.module.css` `.page`(=`<main>`) `padding-top: clamp(96px,14vw,132px)`. **수정**: `app/globals.css` 에 `[data-opbs-chrome] main { padding-top: clamp(48px,7vw,66px) !important }` 추가 → OPBS 콘텐츠(`<div data-opbs-chrome>` 하위 Subpage main) 전부 동일 적용. 브레드크럼 여백(visibility:hidden 박스)은 그대로. **→ 배포 후 파바앱 전 페이지 상단 여백 절반 확인.**

## 0-3. (08-06, ⏳배포대기) 파바앱 회원가입 exist-member → 비밀번호 찾기(재인증)로 변경
사용자 지시 — 파바앱 회원가입 본인인증 후 **이미 아이디 존재(exist-member)** 시, 현재는 `find-id-pw-process`(재인증 없이 비번변경)로 랜딩됨. 이걸 **`find-id-pw-form`(비밀번호 찾기 = 본인인증 화면)** 으로 바꿔 **본인인증을 한 번 더 한 뒤 비번 초기화**하도록. **수정**: `app/(site)/page/brand/common/alert/page.tsx` exist-member 버튼 href `find-id-pw-process` → **`find-id-pw-form`**(라벨 "비밀번호 재설정"→"비밀번호 찾기", instCd·findType=pw 유지). (⚠️ 일반 exist-member 는 반대로 재인증 제거 방향 — 별개 항목.) **→ 배포 후 파바앱 가입 exist-member → 비번찾기 본인인증 → 초기화 관통 확인.**

## 0-4. (08-07, ⏳배포대기) 파바앱 아이디/비밀번호 찾기 3플로우 전체 기능점검·수정
사용자 지시 — 파바앱 회원가입 후 ①exist-member→비번초기화 직행 ②아이디찾기 직접 랜딩→본인인증→아이디확인 ③찾기페이지 pw탭→본인인증→비번초기화, 3플로우 모델API·쿠키·파라미터 정합.

**레거시 대조**: `comm-complete.jsp` exist-member 버튼 = POST `/page/brand/member/find-view.spc`(=초기화 직행, 가입 본인인증 세션 재사용, **재인증 없음**). → 앞서 넣은 재인증(find-id-pw-form) 롤백, 직행 복원.

**핵심 버그(발견·수정)**: 백엔드 `findView`(초기화 조회)·`findComplete`(비번전송) **둘 다 `_AUTH_INFO_TOKEN_` 쿠키**(=MEMBER_AUTH_INFO, 값 확인)를 읽는데, **신규 프론트가 이 쿠키를 브라우저에 안 심어** 비번전송(findComplete)이 항상 H871 실패. lib(process/complete-api)는 `forwardCookies:true`+`bodyMode:"form"` 정상.

**수정(프론트 6곳, 백엔드 무변경)**:
1. `brand/common/alert` exist-member 버튼 → `find-id-pw-form` **롤백** → `find-id-pw-process?...&reqPath=APP` **직행**(라벨 "비밀번호 재설정").
2. `brand/join/policy-bridge` → 가입 authInfo 를 **`_AUTH_INFO_TOKEN_` 브라우저 쿠키**(path=/, httpOnly, 1800s)로 심음 → flow① findView/findComplete 관통.
3. `find-id-pw-process-bridge` → 성공 시 **`_AUTH_INFO_TOKEN_`**(payload.authInfo) 브라우저 쿠키 심음 → flow③ findComplete 관통.
4. `find-id-pw-process/page.tsx` → `reqPath` 기본값 **"APP"**(빈값 시 checkMemberByCi 실패 방지) + 리셋폼 **reqChnl(=instCd) 추가** + **아이디찾기 결과 로그인링크(`/page/login`) → `BrandReturnButton`(창닫기, 파바앱 규칙)**.
5. **리셋폼 GET→POST 전환**(새 비밀번호 URL 노출 방지): 신설 `find-id-pw-complete-bridge/route.ts`(POST→쿠키 `FIND_ID_PW_COMPLETE_COOKIE`→303) + `find-id-pw-complete/page.tsx` 쿠키 우선 읽기(searchParams 폴백).

**검증**: findView 성공=rpsCd00/rpsDtlCd0000→userId+encMbrNo+landingType"find-view"(FindIdPwFlow 성공조건 부합). findComplete=encMbrNo(180일)+newPw일치+`_AUTH_INFO_TOKEN_`재검증+resetPw+메일(레거시 findIdPwResultProcess 이식). **→ 배포 후 3플로우 관통 확인**(특히 flow①③ 비번전송 완료까지). ⚠️ `_AUTH_INFO_TOKEN_` 30분 만료.

## 0-5. (08-07) 휴면(dormancy) 해제 플로우 점검 — 인프라 수정 + ⚠️입력폼/파바앱본인인증 미완
일반/파바앱 휴면 해제 전체 점검(백엔드 계약+프론트 3저장소 스캔). **백엔드 계약 확정**: 해제(dormancy-process/complete)는 ①`_AUTH_INFO_TOKEN_` 쿠키 ②encMnm ③telNo ④mbrNo 강제 + `makeReleaseRequest` 가 **`setOnlnPwd(param.pwd)` // 필수** + 신규 주소(`roadNmPostNo/roadNmAddr`) 등록 → **휴면 해제 = 새 비밀번호 + 주소 입력이 실제 필수**.

**✅ 수정(안전 인프라)**:
- 일반 `auth-process-bridge` → 본인인증 authInfo 를 **`_AUTH_INFO_TOKEN_` 브라우저 쿠키**(path=/page/dormancy, 1800s)로 심음 → 해제 단계에서 백엔드가 재복호화(없으면 none-auth 였음).
- 파바앱 `brand-member-dormancy-view-api`·`dormancy-complete-api` → **`bodyMode:"form"`** 추가(@RequestParam Map, json 이면 미바인딩 실패).

**✅ 입력폼 빌드 완료(08-07, ⏳배포대기)**:
- **신설 `dormancy-view/dormancy-release-form.tsx`(범용 client)**: 새 비밀번호+확인(8~16자·2종 검증) + 주소(Daum 우편번호 임베드, join-info-form 로더 재사용) + 상세주소. props `submitPath`/`successPath`/`extra` 로 일반·파바앱 공용. 제출 시 pwd/newPwChk/roadNmPostNo/roadNmAddr(+extra) → 브릿지 POST.
- **일반**: `dormancy-view/page.tsx` 버튼→입력폼 교체(release-button 삭제). `dormancy-process-bridge`·`dormancy-api`·`DormancyProcessRequest` 에 pwd/주소/mkt(기본N)/reqPage 배선.
- **파바앱**: `brand/member/dormancy-view` 에 동일 폼(submitPath=신설 `dormancy-complete-bridge`, extra={instCd,reqChnl=instCd,reqPath:"APP",reqPage:"dormancy"}). 신설 `dormancy-complete-bridge/route.ts`(POST→`hp_brand_dormancy_submit` 쿠키→303, 새 비번 URL 노출 방지). `dormancy-complete/page.tsx` 쿠키 우선 읽기. complete-api·`DormancyCompleteRequest` 에 pwd/주소/mkt 배선. authInfo 는 **회원가입 본인인증 때 policy-bridge 가 심은 `_AUTH_INFO_TOKEN_`(path=/) 재사용**(별도 cert 불필요 — 회원가입 휴면전환 경로).
- CSP script-src 에 `t1.kakaocdn.net`(Daum) 이미 허용됨. **→ 배포 후 일반·파바앱 휴면: 본인인증→새비번+주소 입력→해제 완주 확인.**
- ⚠️ **잔여**: 14세미만(부모정보) 입력 미구현(추후) · 파바앱 **독립** 진입(`dormancy-auth` 직접, 회원가입 경유 아님)은 본인인증 cert 없어 미지원(회원가입 휴면전환 경로만 커버).

## 0-6. (08-07, ⏳배포대기) 파바앱 전 플로우 감사(탈퇴/정보수정/비번변경/점유인증) — 확정 버그 수정
find/dormancy 에서 나온 공통 버그패턴(bodyMode / POST-to-RSC / 파라미터/쿠키)을 기준으로 3자(프론트↔모델API↔레거시) 감사(에이전트 3). **재검증**: 기존 수정한 find·dormancy = **OK(회귀 없음)**.

**✅ 확정 수정**:
- **bodyMode:"form" 8개 추가**(백엔드 전부 @RequestParam 확인): brand-member `confirm-complete`·`confirm-ownership-proc`·`modify-info-view`·`modify-info-complete`·`modify-pw-view`·`modify-pw-complete`·`withdrawal-view`·`withdrawal-complete` lib. (json 이면 parameters={} 미바인딩) ⚠️ join lib(들)도 json 이나 **join 은 페이지가 직접 form fetch** 라 정상 → 미변경.
- **POST-to-RSC 깨짐 수정**(RSC 페이지는 POST 바디 못 읽음 → 405/유실): `modify-pw-view`·`withdrawal-view` 폼이 complete **페이지로 직접 POST** 하던 것 → 신설 **complete-bridge**(POST→쿠키→303) 경유로 전환 + 완료 페이지 쿠키 우선 읽기. (find-id-pw 와 동일 패턴)
- **withdrawal 폼 파라미터 누락**: `reqChnl(=instCd)`·`reqPath="APP"` hidden 추가(백엔드 @RequestParam 필수).

**검증(에이전트 오탐 정정)**: `modify-pw-view` 는 실제로 비번 입력폼 정상 렌더(에이전트 "폼 없음" = 오탐).

**✅ (08-07) 브랜드 로그인 쿠키·점유인증 쿠키 배관 완료**:
- **핵심 발견**: `WebUtil.setBrandMemberCookie` = `value = instCd+"|"+encAut` **평문**(암호화 아님), 단일 쿠키 `_EXOG_MEMBER_INFO_INSAUT_`. `getBrandMemberCookie` 가 `|` split → INSTCD/ENCAUT. 백엔드 URL_PATH=`/page/brand/member`.
- **Part A (미들웨어 로그인 쿠키)**: `middleware.ts` OPBS 분기에서 진입 URL 에 `encAut` 있으면 **`_EXOG_MEMBER_INFO_INSAUT_ = "{instCd}|{encAut}"`** 브라우저 쿠키 set(path=/page/brand/member, httpOnly, 1800s). → 모든 brand member API(forwardCookies)가 `getBrandLoginCheck` 통과 → **H499 전멸 해소**. (RSC/SSR 은 못 심어서 미들웨어가 유일 해법. 평문이라 미들웨어 재현 가능.)
- **Part B (점유인증 완료 쿠키, 비번확인 경로 HCHP/HWDR)**: confirm-view 폼이 `/api/*` **native POST**(→JSON 노출 UX 깨짐) 였음 → 신설 **`confirm-complete-bridge/route.ts`** 경유로 전환. 성공 시 **`_EXOG_MEMBER_CONFIRMED_ = redirectUrl`**(평문, path=/page/brand/member, 24h) 심고 목적화면(HCHP→modify-pw-view / HWDR→withdrawal-view, instCd 부착)으로 303. 실패 시 confirm-view 재노출. → **비번변경·탈퇴 전체 체인 완주 가능**(미들웨어 로그인쿠키 → 비번확인 → 점유쿠키 → view → complete-bridge).

**🚧 남은 갭**:
1. **정보수정(HMOI) 경로**: ①점유인증이 **SMS(confirm-ownership-proc)** 방식(비번확인 아님) — 아직 `/api/*` native POST(JSON UX) + SMS 코드 검증 서브플로우 미완. ②`modify-info-view` **편집 폼 스텁**(읽기전용+GET저장, 주소/이메일/수신동의 입력 없음) → dormancy 처럼 편집폼 빌드 필요. → **정보수정만 별도 빌드 남음**(비번변경·탈퇴는 위 Part A/B 로 커버).

## 1. SSR 로그인 판별 (전 페이지)
- `lib/auth-server.ts` `getCurrentUser()` — 모든 요청 쿠키(JSESSIONID)를 백엔드 `GET /api/auth/check` 로 전달, 타임아웃 4초, 오류/타임아웃/미로그인 → null.
- `components/auth/auth-provider.tsx` — SSR 판별값을 클라 컨텍스트로 전달(`useAuth`). `auth-actions.tsx` 가 이걸로 로그인/로그아웃 UI + 로그아웃 BFF(`/api/logout`).
- 루트 `layout.tsx` `export const dynamic = "force-dynamic"` — 전 페이지 동적 렌더 강제 → 정적 프리렌더로 check 를 건너뛰던 문제 해결(`/`·presentation 등 포함).
- `check` 규약: HTTP 항상 200, `code:"00"`=로그인 / `code:"50"`=미로그인(`ApiResponseWrapper`+`ApiError.unauthorized`). 백엔드 `me`→`check` 리네임.
- 원요청 파라미터를 check 로 전달: 미들웨어가 `x-hp-query`/`x-hp-method` 헤더로 넘기고 `getCurrentUser` 가 check URL 에 쿼리 부착 + 로그.

## 2. 인증 3종 통일
- `POST /api/auth/login` · `POST /api/auth/logout` · `GET /api/auth/check` (기존 me).

## 3. KCB 본인인증(휴대폰·아이핀) 연동 ★
- **방식**: React가 백엔드 JSP 팝업을 열고(window.open), 완료 시 `postMessage` 로 결과 수신. 크립토·복호화는 100% 백엔드.
- **프론트**: `join/index/join-auth.tsx`(client) — 휴대폰/아이핀 버튼 → 팝업(500×800) → `message` 리스너. 성공(phone `B000`/ipin `T000`) → 결과 sessionStorage 저장 + `/page/join/policy` 이동. `subpage.tsx` `AuthMethod` 에 `onSelect` 추가.
- **백엔드 JSP**: `cert/kcb/{phone,ipin}/complete.jsp` 에 `window.opener.postMessage(result,'*')` 추가(기존 `opener.page.authCallback` 유지 → JSP 부모 호환).
- **팝업 파라미터**(레거시 join-auth.jsp 동일): `reqChnl=NONE, reqPage=join, reqPath=`.
- **결과 로깅**: `join-auth` → `/api/cert/log`(신규 route) POST → `apiLog` 서버 파일 로그 + 브라우저 콘솔. ⚠️ 디버깅용, 완료 후 제거 권장(개인정보).

## 4. /api 라우팅 통일 (ELB: /api/* → 백엔드)
- 본인인증 호출·콜백 URL 전부 `/api/page/cert/...` 로:
  - 프론트 팝업 `join-auth.tsx`, 레거시 `unvus.util.js`(callPhonePopup/callIpinPopup), KCB `config.jsp` RETURN_URL, NICE `config.jsp`(FAIL/PROCESS/RETURN).
  - 컨트롤러 `CertController`·`CertPhoneController`·`CertIpinController` → `@RequestMapping("/api/page/cert")` (구 `/page/cert` 제거).
  - `SitemeshFilter` `/api/page/cert/*` 제외(+`/api/*`), `DeviceRedirectFilter` `/api/**` 커버. 구 `/page/cert` 제외 라인 제거.
- ⚠️ **전제**: ELB 가 `/api` 를 strip 하지 않고 그대로 백엔드 전달. strip 하면 컨트롤러를 `/page/cert` 로 되돌려야 함 → 배포 후 팝업 도달로 검증.

## 5. 서버 요청/응답 파일 로그
- `lib/log.ts` `apiLog` — 파일(`LOG_DIR`) + 콘솔. 날짜별 `ha-web-fo-YYYYMMDD.log`. 길이 4000자 캡. 로컬=`D:/logs/ha-web-fo`, 서버(dev/stg/prod)=`/app/server/ha-web-fo`. (운영 포함 전 환경 기록; `LOG_ENABLED=false` 로만 off)
- 계측: `legacyGet`(계약 API 성공/실패 본문), `fetchLegacy`(레거시 스크래핑), `/api/login`·`/api/logout`(비번 마스킹), `getCurrentUser`(check).
- **모든 페이지 진입 로그**: 미들웨어 `x-hp-req` 헤더 → 레이아웃 `apiLog("[page]", ...)` (GET 파라미터 포함). POST 바디는 RSC에서 못 읽어 미기록(BFF/route 에서 개별 로깅).

## 6. 레거시 authInfo 흐름 파악 (다음 단계 근거)
- `BrandMemberController`: `POST /join-policy.spc` 가 authInfo(암호문) 받아 `AES128Util.decrypt(cert.key)` 복호화 → `MEMBER_AUTH_INFO` **HttpOnly 쿠키(30분)** 로 저장. `join-view.spc` 가 그 쿠키로 이름/생년/휴대폰 복호화해 폼 채움.
- 시사점: authInfo·CI/DI 는 프론트가 다루지 않고, **약관동의 API 에 authInfo 넘기면 백엔드가 HttpOnly 쿠키로 유지**하는 구조로 React 연동해야 함.

## 7. (07-29) 로그인 체크 API 최소화
- `/api/auth/check` 응답을 **"로그인 여부"만**으로 축소 결정. 백엔드는 로그인 시 `{code:"00", data:{loggedIn:true}}`, 미로그인 `code:"50"`.
- 프론트 `lib/auth-server.ts`: `getCurrentUser` → **`checkLoggedIn(): Promise<boolean>`** (개인정보 미수신). `auth-provider.tsx`/`layout.tsx` 는 로그인여부만 클라이언트 전달.
- 원리 정리: SSR fetch 자체는 브라우저 미노출이나, **AuthProvider(클라이언트 컴포넌트)로 넘기는 값이 곧 유출 지점** → 최소 필드만 전달. 개인정보는 렌더 후 사용자 액션 시점에 백엔드 직접 호출로 취득.
- ※ 이후 워킹트리에서 `AuthProvider`가 GA/Amplitude용 `mbrNo` 포함 버전으로 되돌아옴(seokej 라인) — 최종 필드 범위는 정합 필요.

## 8. (07-29) dev 도메인 정리
- `dev-www.happypointcard.com` → **`dev.happypointcard.com`** 전체 치환: 프론트 `.env.local`/`.env.dev`(`LEGACY_BASE`·`LEGACY_BASE_FALLBACK`·`NEXT_PUBLIC_API_BASE`), `.env.stg`/`.env.prod` 주석.

## 9. (07-29) 로컬 로그인 — BFF 프록시 방식으로 일원화 ★
- **문제**: 로컬(localhost:3000) → dev 백엔드 크로스오리진 로그인이 CORS/Secure쿠키로 실패. `NEXT_PUBLIC_API_BASE` 유실로 self(localhost) 호출되던 버그도 확인.
- **검토①(폐기) 백엔드 회사IP CORS**: dev/stg 한정 + 회사IP(14.32.109.30)에서만 localhost CORS/CSRF예외/SameSite=None. → nginx preflight 403·Secure쿠키·배포 불일치로 난항. **커밋 삭제·원복**.
- **채택 = 프론트 BFF 프록시** (seokej `7977955`, `app/api/auth/[...path]/route.ts`): 브라우저는 동일오리진 `/api/auth/{login,logout,check}` 만 호출 → Next 서버가 `LEGACY_BASE` 로 중계 + `Set-Cookie`의 `Domain`/`Secure` 제거(로컬). **서버-투-서버라 CORS/CSRF 완화 불필요**.
- 결론: **백엔드 인증 커스터마이징 0** (원본 `SecurityConfig` = permitAll + 전역 CSRF). 로컬 로그인은 프론트 프록시 단독으로 완결.

## 10. (07-29) 로그인 보안 논의 (설계 단계, 미구현)
- 서버-투-서버 로그인은 원리상 완전차단 불가(브라우저 신호 위조 가능). 남용 방지가 목표 → CAPTCHA(폼에 자리 존재)·rate limit·계정 잠금·WAF.
- DynamoDB **nonce** 검토: 리플레이 차단·발급지점 방어훅으론 유효하나 봇 차단은 불가 → **세션/IP 바인딩 + single-use TTL + rate limit + CAPTCHA 결합** 조건 하에서만 실효.

## 11. (07-31) 회원가입 흐름 정리 — 공통 alert·필드 리네임·브랜드 라우트 이동
- **공통 안내 페이지**: `/page/join/popup` → **`/page/common/alert`** 이관(구 경로는 완전 이동 아님/리다이렉트 스텁 유지). alert 랜딩 규격(type 5종·문구·버튼)은 이 페이지가 **정본(SSOT)**, 백엔드는 값만 내려줌.
- **백엔드 `/api/join/policy` 응답 변경**: `landingType`→**`alertType`**, `landing` 유지, 요청 컨텍스트 `reqPath`/`reqChnl`/`reqPage` **전 분기 항상 포함**(없으면 빈값). 주석은 alert 규격 제거 후 프론트 SSOT 포인터만.
- **디버그 제거**: `join-auth.tsx`의 클라이언트 `console.log("[cert]", data)`(개인정보) + 죽은 `/api/cert/log` fetch 제거 → 클라이언트 `console.log` 0건. (서버 `apiLog`는 유지, `next start`라 브라우저 미노출)
- **as/ad**: 앰플리튜드(분석) 전용 → 회원가입 policy 체인엔 **넘기지 않음**(확정). 레거시 model명 `app_as`/`app_ad`, 신규 API 응답 `appAs`/`appAd`, 요청 파라미터는 공통 `as`/`ad`.
- **브랜드 본인인증 라우트 이동**: `/page/brand/member/join-auth` → **`/page/brand/member/join/index`** (완전 이동, 하위호환 스텁 없음). 참조 갱신: join-gate CTA, middleware `OPBS_REDIRECT_MAP`(`/page/join/auth.spc`→새 경로), canonical, error 태그, interface 주석. 백엔드 계약 API `/api/brand/member/join-auth`·lib는 **불변**.
  - ⚠️ 브랜드 `join/index` 페이지는 아직 **KCB 팝업 미연동(정적 placeholder)** — 일반 `join-auth.tsx`의 `openCert` 이식 필요(미결).

## 12. (07-31) 파바앱(브랜드) 본인인증 팝업 이식 — 일반과 정합 ★
- **레거시 확인**: 브랜드 `join-auth.spc`는 `instCd`를 **INSAUT 쿠키**로 저장, cert 팝업(config.jsp)은 `reqChnl/reqPage/reqPath`만 읽음(instCd 안 읽음). 즉 팝업 파라미터 세트는 일반과 동일, **reqChnl 자리에 브랜드 코드(instCd)** 를 싣는 구조.
- **일반 `join-auth.tsx`**: 팝업 `reqChnl` `"NONE"` → **`channel`(pc/mo, 없으면 빈값)**.
- **신규 `brand/member/join/index/join-auth.tsx`**(일반 포팅): cert 팝업 `reqChnl=instCd`·`reqPage=join`·`reqPath=OPBS`. 성공 → form POST → `/page/brand/member/join-policy` (hidden: authInfo/reqPath=OPBS/reqPage=join/reqChnl=instCd/**instCd**). `brand/member/join/index/page.tsx`는 정적 → `BrandJoinAuth` 렌더(휴대폰만), `getJoinAuth` SSR 검증 유지.
- **미들웨어**: `POST /page/brand/member/join-policy` 브랜치 추가 → `hp_join_auth` 쿠키(+instCd) 저장 후 303 GET(일반과 동일 방식).
- **브랜드 `join-policy/page.tsx`**: searchParams → **쿠키(hp_join_auth)에서 authInfo/instCd/reqPath 읽어 `postJoinPolicy`** 호출로 정합(쿠키 없으면 searchParams 폴백).
- **백엔드 `BrandMemberModelApiResource.joinAuth`**: `reqPath` `"APP"`→**`"OPBS"`**.
- 확정 규칙: 일반 reqChnl=pc/mo(없으면 빈값), 파바앱 reqChnl=instCd. as/ad는 앰플리튜드용(policy 미전달).

## 13. (07-31) 브랜드 약관 라우트 이동 + as/ad 앰플리튜드 버그 수정
- **라우트 완전 이동**: `/page/brand/member/join-policy` → **`/page/brand/member/policy`** (하위호환 스텁 없음). 갱신: 이동된 page/error, `join/index/join-auth.tsx` form.action+주석, `middleware.ts`(브랜드 policy POST 브랜치 pathname·쿠키 path), interface 주석. 백엔드 API `/api/brand/member/join-policy`·lib는 불변.
- **as/ad 앰플리튜드 연결 버그 수정**: `analytics-provider.tsx`가 URL에서 `app_as`/`app_ad`(레거시 **model 속성명**)를 읽어 `setSessionId`/`setDeviceId`가 **한 번도 동작 안 하던** 문제 → 실제 URL 파라미터 **`as`/`ad`** 로 교체. 앰플리튜드 초기화는 **전역 프로바이더 단일 지점**(중복 없음)이라 이 한 파일로 전량 수정됨.

## 14. (07-31) 룰셋 "1 page ↔ 1 model API" 확립 + 그룹1 배선
- **룰셋 확정**: 모든 프론트 page.tsx는 진입 시 대응 모델 API 1회 호출(데이터 불필요해도). `lib/model-ping.ts` `pingModel()` 신설(fire-and-forget). 없으면 백엔드 생성(성공만 반환). → INDEX 아키텍처 핵심 + 메모리(page-model-api-rule)에 저장.
- **전수조사**: 79개 page 중 33개 모델 API 미호출 → 대응 API 존재(그룹1 17~18) / 이름확인 필요(그룹2 8) / 정적(그룹3 8)로 분류.
- **그룹1 배선(진행)**: 18개 page에 `pingModel("/api/...")` 추가. POST 전용 3개(join/form·join/optional-form·member-info/modify-info-form)는 백엔드에 **진입 핑용 GET 엔드포인트**(빈 `LinkedHashMap` 성공) 추가. 백엔드 컴파일 ✅.
- **`/api/join/index`**: 파라미터 에코 제거(빈 data 반환) — 프론트는 URL에서 직접 읽으므로 불필요.
- 그룹2·3은 사용자 검토 대기.

## 15. (07-31) 그룹2 배선 — 신규 모델 API 6개 + 스텁 + 핑
- **백엔드 신규 GET 모델 API 7개**(전부 성공만 반환 스텁, `com.spc.hpc.api.model.*`): `event/my-coupon`, `search`(page/search용), `dormancy/auth-form`, `member-info/{withdrawal-form, find-id-pw-form, confirm-pw-form, change-pw-form}`. 컴파일 ✅.
- **`page/search`**: 삭제하려 했으나 **실사용 검색 결과 페이지**(header-search·mobile-header·site-nav·layout SearchAction·robots 참조) 확인 → 삭제 대신 빈 모델 API(`/api/search`) 신설·연결로 방향 전환.
- **프론트 그룹2 8개 page** `pingModel` 배선: `join/auth`→`/api/join/index`, `event/my-coupon`, `dormancy/auth-form`, `search`, `member-info/{withdrawal-form,change-pw-form,confirm-pw-form,find-id-pw-form}`.
- ※ 신규 6개는 현재 **성공만 반환하는 스텁**. 레거시 컨트롤러(MemberInfoController form 계열·DormancyController·couponService) 실로직 이식은 후속 과제.

## 16. (07-31) main 머지 + 스테이징 도메인 정합
- **머지**: `feature/WORK-16613` → `main`(커밋 `a31b9de`). 충돌 10파일 해결(중요: `lib/legacy-http.ts` git auto-merge 함수중복 → main버전 복원). main 실기능 보존 + 16613 산출물(pingModel·브랜드흐름·analytics·common/alert) 전량 반영. tsc 신규에러 0(main baseline 17=머지 17). push는 미실행.
- **스테이징 도메인**: 프론트 `.env.stg` `LEGACY_BASE` `www`(오류)→**`stg-www.happypointcard.com`** + `LEGACY_BASE_FALLBACK`·`NEXT_PUBLIC_API_BASE=""` 추가. 백엔드 `application-stage.yml`은 이미 stg-www(불변). 구동=PM2 `ha-web-fo` `pnpm start:stg`.

## 17. (07-31) `/page/auth/login` 개발 서버 404 수정
- **증상**: `app/(site)/page/auth/login/page.tsx` 파일과 Next 개발 산출물은 존재했지만, 로컬 `pnpm dev`에서 `/page/auth/login`이 404로 응답.
- **원인/조치**: 해당 페이지가 정적으로 최적화되는 경로로 처리되어 `(site)` 라우트 등록이 불안정했다. 로그인 상태 확인은 요청 단위 SSR이므로 `page.tsx`에 `export const dynamic = "force-dynamic"`을 명시했다.
- **검증**: `http://localhost:3000/page/auth/login` HTTP 200, 페이지 제목 `로그인 | Happy Point` 및 로그인 폼 렌더링 확인. `/api/auth/check`도 정상 200 응답.

## 18. (07-31) 모델 API param/model 디버그 로깅 (운영 제외)
- `lib/model-ping.ts` `pingModel()`: 모델 API 호출 후 **받은 파라미터 + 모델 응답**을 `apiLog`로 출력.
  - 형식: `[model] <path>` / `param=<x-hp-query & x-hp-body>` / `model=<응답 JSON>`.
  - param 출처 = 미들웨어가 심은 원요청 GET쿼리(`x-hp-query`) + POST바디(`x-hp-body`) → **페이지 수정 0**(24개 page가 이미 `pingModel` 호출).
- **노출 게이트**: `process.env.APP_ENV !== "prod"` → local/dev/stg 노출, **운영만 제외**(개인정보 보호). apiLog 경유(서버 로그파일 + 콘솔).
- tsc: 신규 에러 0(baseline 17 유지).

## 19. (07-31) 메인 일반 배너 계약 API 연동
- 홈(/, PC/모바일 공통) 하단의 "지금 가장 인기 있는 이벤트만 모았어요" 마키를 정적 배너 대신 새 배너 API 응답으로 교체.
- AppHomeBanner가 서버에서 세션 쿠키를 전달해 호출하고, 이미지·브랜드·제목·부제목·링크를 카드에 렌더링한다. Oracle/MyBatis Map 직렬화의 대문자 키도 함께 수용.
- 각 카드에 API의 모든 원시 필드를 `data-*`로 자동 전개한다(예: `BN_INFO_ID` → `data-bn-info-id`). `IMG_URL`/`imgUrl`은 HTTP(S)·상대경로 검증 후 실제 img 태그에 적용.
- 후속 정합: 실제 응답의 스네이크 케이스(`img_url`, `brand_nm` 등)도 카멜/대문자 키와 동일시해 읽도록 키 정규화. 데이터 속성만 보이고 이미지·문구가 비던 현상을 해결.
- 배너 카드의 `promo-card-text` 텍스트 오버레이를 제거하고 이미지 단독 노출로 확정. `data-*`와 링크 동작은 유지.
- 링크는 상대 경로 또는 HTTP(S)만 허용하며 외부 링크는 새 탭으로 연다. 조회 실패 또는 빈 목록이면 섹션을 숨긴다.
- 검증: pnpm exec tsc --noEmit 실행. 이번 변경 오류 0건, 기존 회원정보/마이페이지 타입 오류 16건으로 전체는 실패.

- 배너 클릭은 `returnUrl` 없이 `/page/auth/login?bninfoid=<BN_INFO_ID>&linkvalue=<LINK_VALUE>`로 이동한다. 로그인 GET 랜딩 시 middleware가 두 URL 값을 동일 이름의 `httpOnly` 쿠키(`SameSite=Lax`, `Path=/`, 30분)로 저장해 후속 로그인 처리에서 사용할 수 있게 했다.

## 20. (07-31→08-03) 세션 stickiness — ALB 앱쿠키 방식 (Redis 미사용) ★프론트/백엔드 구현 완료, dev 검증 대기
- **증상(스테이징)**: ALB 스티키를 duration(ELB세션)으로 뒀는데, **프론트 SSR→백엔드** 구간이 스티키로 못 붙어 JSESSIONID 세션 미스. 프론트는 한쪽에 붙지만 백엔드는 라운드로빈.
- **원인**: ALB 스티키는 브라우저가 쿠키를 저장·재전송해야 유지되는데, **SSR(front 서버)은 브라우저가 아니라 쿠키잼이 없어** 매 요청 새 방문자로 인식 → 라운드로빈.
- **확정 구성(08-03, 스테이징 ELB 셋팅 완료)**: 도메인 `stg-www.happypointcard.com`.
  - **프론트 TG(ha-web-fo EC2 2대)** = ELB **세션 스티키**(쿠키 `AWSALB`). 프론트는 무상태라 자체 스티키는 정합성 무관 — 신경 안 씀.
  - **백엔드 TG(ha-web-api EC2 2대)** = **애플리케이션 기반 쿠키 스티키**, 앱 쿠키명 **`HA_AWSALB`**, duration 1h, 알고리즘 round robin. (`stg-app-web-api-tg`, HTTP:8021)
  - 쿠키명 분리(`AWSALB`≠`HA_AWSALB`)로 동일 도메인 충돌 없음.
- **핵심 이해**: AWS 애플리케이션 기반 스티키는 **LB 가 쿠키를 만들지 않는다.** 타겟(ha-web-api)이 `HA_AWSALB` 를 직접 Set-Cookie 해야 ALB 가 `AWSALBAPP` 를 발급하고 이후 요청을 같은 타겟으로 고정. → **백엔드가 앱 쿠키를 발급하는 게 필수 전제.**
- **구현 (08-03)**:
  - **[백엔드]** `com.spc.hpc.home.filter.StickinessCookieFilter`(OncePerRequestFilter) 신설 — 요청에 `HA_AWSALB` 없으면 `UUID` 값으로 발급(Path=/·HttpOnly·Secure(X-Forwarded-Proto=https 포함)·MaxAge 1h). `web.xml` 에 `hstsFilter` 다음으로 `/*` 매핑 등록.
  - **[프론트]** SSR→백엔드 쿠키 forward 통일 — 요청 쿠키 전체(`cookies().toString()`)를 전달하므로 `AWSALBAPP`+`HA_AWSALB`+`JSESSIONID` 자동 커버.
    - `lib/legacy-http.ts`: `legacyContractGet/Post` 의 `forwardCookies` **기본값 true** 전환(명시적 false 는 존중, no-store 라 캐시 오염 없음). → `pingModel` 포함 전 계약 호출 자동 forward.
    - `app/(site)/page/join/policy/page.tsx`: raw fetch 에 `cookie: store.toString()` 추가(유일 미forward raw fetch).
    - 이미 forward: `getCurrentUser`(auth-server), `join/form/page.tsx`.
  - **[env]** `.env.stg` 에 `LEGACY_BASE=https://stg-www.happypointcard.com` 기존 존재 확인 ✅.
- **검증(스테이징 배포 후, 미완)**: ① 브라우저 쿠키에 `HA_AWSALB`/`AWSALBAPP` 생성 확인 ② 백엔드 healthy 타겟 **2대** 등록 상태에서 로그인 후 페이지 이동 시 동일 인스턴스 세션 유지 확인(현재 TG target 1대만 보임 → 2대 healthy 필요). ③ tsc: 수정 파일 에러 0(기존 stale `.next/dev/types` 브릿지 참조 1건은 clean 빌드 시 소멸).
- **🔴 스테이징 검증 결과 (08-03) — 2B(구조적) 확정, 로그인 토글 발생**:
  - 증상: 로그인 성공 후 새로고침하면 로그인/로그아웃 버튼이 왔다갔다. 백엔드 2대 액세스 로그상 **SSR(로그인체크·모델API)만 1·2번 서버 번갈아 호출**, 브라우저 직접 호출(`/api/auth/login`)은 2번 서버로만 고정. 세션(JSESSIONID)은 2번에만 생성 → SSR 체크가 1번 갈 때 미로그인으로 보여 토글.
  - 쿠키 확인: 브라우저에 `HA_AWSALB`(필터 발급 UUID)·`AWSALBAPP-0`(ALB)·`AWSALB`(프론트)·`JSESSIONID` **전부 정상 생성**. → 인프라·필터 설정 정상.
  - **진단 로그(`getCurrentUser` non-prod)**: SSR 이 백엔드로 forward 하는 Cookie 헤더에 `HA_AWSALB=true AWSALBAPP=true AWSALB=true JSESSIONID=true` — **넷 다 실려감**. 그런데 **동일 쿠키를 실은 두 check 요청이 각각 다른 백엔드로 라우팅**(하나 `isAuthenticated:false`, 하나 `true`). → **2A(forward 누락) 배제, 2B 확정.**
  - **결론**: ALB 애플리케이션 쿠키 스티키는 **브라우저 직접 요청에는 적용되나, 프론트 서버(BFF)가 대리 전송한 요청에는 적용되지 않는다.** ALB 쿠키 스티키 + BFF(SSR이 세션을 대리 운반) 구조의 근본적 불일치.
  - **채택 방향(08-03, 사용자 제안)**: Redis 대신 **암호화 인증 쿠키(무상태/세션 부트스트랩)**. 로그인 시 백엔드가 사용자정보를 AEAD 암호화 쿠키로 발급 → 어느 인스턴스로 가도 쿠키 복호화로 로그인 판별 → 스티키 불필요. ⚠️ 핵심: 로그인체크만이 아니라 **세션 의존 다운스트림 API 전부 커버**하도록 "세션 재구성 필터"로 설계해야 함(아니면 버튼만 로그인·데이터는 실패). 보안: AEAD+만료+HttpOnly/Secure/SameSite+키공유.
  - **사전작업(08-03)**: 쿠키 `Secure` 환경설정값 추가 — `application.yml` `cookie.secure: true`(기본, dev/stage/stagep/prod), `application-local.yml` `cookie.secure: false`(로컬 http). 향후 인증쿠키·`StickinessCookieFilter` 가 이 값을 읽도록 배선 예정.
  - **ALB 스티키 코드 원복(08-03)**: 방향 전환에 따라 스티키 실험 코드 제거 — 백엔드 `StickinessCookieFilter.java` 삭제 + `web.xml` 등록 원복, 프론트 `getCurrentUser` 스티키 진단 로그 원복. **유지**: ① `cookie.secure` 환경설정값(암호화 쿠키에도 필요) ② 프론트 SSR 쿠키 forward 통일(`legacyContractGet/Post` 기본 ON + `join/policy` cookie) — BFF가 인증쿠키를 백엔드로 전달하는 데 필수라 존치(코멘트를 스티키→세션/인증 기준으로 정리). ALB TG 스티키/HA_AWSALB 인프라 설정은 콘솔에서 해제 가능(암호화 쿠키 방식은 스티키 불필요).
  - **로그인 정책 확정(08-03)**: **1단계 = 세션유저 암호화 쿠키(권장: AES-GCM/AEAD — 암호화+무결성 동시)**, **2단계(운영 후) = JWT 등 서명 검증 포함 표준화(권장: JWS 단독은 페이로드 노출이므로 JWE/서명+암호화로 프라이버시 유지)**. 공통 전제: 로그인체크만이 아니라 **세션 의존 다운스트림 전부 커버하는 세션 재구성 필터** + exp/짧은 TTL/로그아웃/키공유·회전.
  - **다음**: 백엔드 로그인 발급 지점 + `/api/auth/check`·세션 사용처 조사 → 1단계(암호화 쿠키+세션 재구성 필터) 스펙·필터 위치·암호화 방식 설계.
  - **1단계 설계 확정(08-03)**: 방식=**세션 유지(부트스트랩)** — 로그인 시 세션(authToken=SessionUser) 유지 + **SessionUser 전체를 암호화한 `_HPC_AUT` 쿠키 발급**, 이후 요청은 세션에 authToken 없으면 쿠키 복호화로 세션 재구성. 완전 무상태(세션 제거)는 2단계 TODO. 확정값: 암호화=**AES-256-GCM**(IV 매번 랜덤), 페이로드=**SessionUser 전체**(배포 후 쿠키크기 모니터링), 쿠키명=**`_HPC_AUT`**, 만료=**슬라이딩 60분**(무활동 기준, 현행 `setMaxInactiveInterval` 정합), 속성=HttpOnly+Secure(`cookie.secure`)+SameSite=Lax+Path=/.
  - **KMS 키 획득 경로 분석(08-03)**: `application.yml aws.kms.{id,access-key-id,secret-access-key,encrypt-val}` → `AwsKmsUtil.decryptKeyData()`(AWS KMS.decrypt) → `KMSConfig`(@PostConstruct 1회) 가 복호화된 JSON `{name:{key,iv},...}` 파싱 → `getKeysMap()` 으로 `"hpc.key"/"cert.key"/"oilbank.key"...` 제공. 기존 소비는 전부 `AES128Util`(AES/CBC). **인증쿠키 키도 `KMSConfig.getKeysMap()` 재사용 방침.**
  - **⚠️ AES-256/GCM 이슈 2건**: ① 256bit 키 → **해결: `kmsConfig.getKeysMap().get("sucToken.key")`(AES-256 KMS 키) 사용 확정.** ② **GCM은 고정 iv 재사용 금지**(치명적) → `sucToken.key`만 쓰고 **IV는 매 암호화 랜덤 생성**해 쿠키에 동봉. `sucToken.iv`는 GCM nonce로 **미사용**(필요 시 AAD 보조용만).
  - **암호화 확정(08-03)**: AES-256-GCM / 키=`kmsConfig.getKeysMap().get("sucToken.key")` / IV=랜덤 96bit(쿠키 동봉) / 태그 128bit. 쿠키값=base64(IV12+암호문+태그16). KMS 키는 `KMSConfig`(@PostConstruct)가 앱 시작 시 `encrypt-val` 복호화로 로드한 것 재사용.
  - **timestamp 필드 추가(08-03)**: `SessionUser`에 `private String timestamp=""`(epoch millis) 추가, 로그인 시 `System.currentTimeMillis()` 세팅(`AuthApiResource`). **용도=세션 최초생성 시각 → 암호화 쿠키 만료(exp)/슬라이딩 판정.** (IV 유일성은 랜덤 IV가 담당; timestamp는 nonce 아님 — GCM 고정IV/nonce재사용 위험 회피 확정. CBC+타임스탬프 방식은 위치·유일성·무결성 문제로 폐기.)
  - **1단계 구현 완료(08-03, ha-web-api, BUILD SUCCESS)**:
    - `home.security.AuthCookieService`(@Component) 신설 — AES-256-GCM 암복호화(`encrypt/decrypt`), 발급(`issue`, timestamp=now 갱신), 삭제(`clear`), 만료(`isExpired`)·슬라이딩판정(`needsSlidingRefresh`, 만료 절반 경과 시만 재발급→Set-Cookie 남발 방지). 키=`kmsConfig.getKeysMap().get("sucToken.key")`, IV=랜덤96bit, 값=base64url(IV+ct+tag), 만료=`sso.timeout`분, Secure=`@Value("${cookie.secure:true}")`.
    - `AuthApiResource`: login 성공부 `authCookieService.issue(sessionUser, response)`, logout `authCookieService.clear(response)` (메서드에 HttpServletResponse 추가).
    - `home.filter.AuthBootstrapFilter`(OncePerRequestFilter) 신설 — 세션에 authToken 없고 `_HPC_AUT` 유효하면 복호화→세션 복원(만료 시 쿠키삭제, 유효 시 슬라이딩 재발급). 빈은 `WebApplicationContextUtils`로 루트컨텍스트에서 지연획득. `web.xml`에 `spcFilter` **앞** 매핑.
    - `UserService`(이름변경 후 세션 갱신): `authCookieService.issue(sessionUser, WebUtil.response())` 로 쿠키 스냅샷 최신화.
    - 검증: `mvn -o compile` BUILD SUCCESS. **미검증**: 런타임(로컬 로그인→복원, 스테이징 2대 토글 해소), 쿠키 크기 실측, `sucToken.key` 실제 32byte 여부(아니면 encrypt 실패 로그).
    - **명칭(기능명)**: **"로그인 인증 암호화 토큰(`_HPC_AUT`)"** — 세션 무상태화(암호화 쿠키 기반). 페이로드=SessionUser, 알고리즘=AES-256-GCM. (대안 호칭: "로그인 인증 암호화 페이로드")
    - **[진행중] 08-03 스테이징 배포 중** → 배포 후 검증: ① `[_HPC_AUT] 암호화 실패` 로그 유무(키 32byte 확인) ② 로그인 후 새로고침 반복 시 백엔드 2대 어디로 가도 로그인 유지(토글 해소) ③ 브라우저 쿠키 `_HPC_AUT` 생성·크기 ④ 로그아웃 시 쿠키 삭제.
    - **✅ 08-04 스테이징 실동작 성공** — 로그인 후 새로고침 토글 사라짐(무상태 인증 정상 작동). 스티키 아직 켜진 상태라 스티키 해제(라운드로빈) 검증은 별도 TODO로 등록(아래). 추가 관찰 지속 예정.

## 22. (실패·시행착오·롤백 기록 — 세션 08-03~04)
> 로그인 세션 무상태화(§20)에 이르기까지의 폐기/롤백 경로를 남긴다(같은 시도 반복 방지).
- **❌ ALB 앱쿠키 스티키(`HA_AWSALB`) 방식 — 폐기(코드 원복)**: `StickinessCookieFilter` + web.xml + 프론트 forward + `getCurrentUser` 진단로그까지 붙였으나, **스테이징 검증 결과 2B(구조적 한계)** 확정 → ALB 앱쿠키 스티키는 브라우저 직접요청엔 적용되나 **프론트(BFF) 대리요청엔 미적용** → SSR 라운드로빈으로 토글. 스티키 관련 코드 전부 **원복**(필터 삭제·web.xml 복구·진단로그 제거). **유지분**: `cookie.secure` 설정값, 프론트 SSR 쿠키 forward(암호화쿠키에도 필요). → 방향을 암호화 쿠키(무상태)로 전환(§20).
- **❌ 암호화 IV 설계 시행착오 — 모두 기각**: ⓐ KMS 고정 `sucToken.iv`를 GCM에 사용(=nonce 재사용, 치명적) ⓑ CBC+고정IV+timestamp/난수프리픽스로 랜덤화(위치·유일성·무결성 결여, HMAC 별도 필요) ⓒ 랜덤IV를 고정키+고정iv로 재암호화(IV는 비밀 불필요→이득0+고정IV 재도입). → 최종 **AES-256-GCM + 매번 랜덤 IV 동봉**(IV 공개는 표준). `SessionUser.timestamp`는 nonce가 아니라 만료판정용으로만 채택.
- **↩️ 수신동의 강제 게이트 미사용 처리 → 즉시 원복**: `need_reception_agree` 게이트 4곳(`AuthApiResource`·`SpcInterceptor`·`ReceptionAgreeController`·`ReceptionAgreeModelApiResource`) 주석처리했다가 **사용자 요청으로 전부 원복**(현재 활성). 레거시 `ha_web`에도 동일 게이트 실사용 확인 → 정식 기능이라 유지.
- **↩️ my-point 게이팅**: AuthGate 인라인 ↔ redirect(returnUrl) 방식 사용자 원복 반복 후, 최종 **미로그인 시 `/page/auth/login?returnUrl=/page/mypage/my-point` redirect**로 확정.

## 26. (08-04) notice-view / voteView 500 — sanitize-html serverExternalPackages 누락
- **증상**: `https://stg-www…/page/customer/notice-view?seq=4215` (및 dev) "Internal Server Error"(raw 500, text/plain).
- **진단**: 백엔드 `/api/customer/notice-view` 는 **HTTP 200 정상 JSON**(백엔드 무죄). 프론트 SSR 페이지만 500. **모든 seq(유효·없음·`abc`)가 500** → 데이터가 아니라 **라우트 모듈 로드 실패**(에러 경계 밖). `sanitize-html` 을 쓰는 라우트만 전부 500(**notice-view + donation/voteView**), 안 쓰는 라우트(notice-list/faq/store/alliance/donation-intro·list)는 200. **dev·stg 둘 다 동일** → 배포 설치 문제 아님, **프로덕션(`next build`) 번들 이슈**.
- **원인**: `lib/legacy-sanitize.ts` 가 `sanitize-html`(+htmlparser2 등) import. Next 서버 번들에 인라인되면 프로덕션 런타임 로드 실패 → 해당 라우트 모듈 초기화 시 throw → 매 요청 500.
- **수정**: `next.config.mjs` 에 **`serverExternalPackages: ["sanitize-html"]`** 추가(번들 제외·런타임 node_modules require). config 파싱 검증 OK.
- ⚠️ **재빌드 필수**: config 변경 → dev/stg/운영 **`pnpm build` 후 재기동**해야 반영. 재배포 후 notice-view·voteView 200 확인.

## 25. (08-04) 회원탈퇴 안내 — withdrawal-info → withdrawal-form 통합 (1 page↔1 model API 정합)
- **문제**: 탈퇴 화면이 모델 API를 2개 호출 — `pingModel(/api/member-info/withdrawal-form)`(빈 껍데기) + `getWithdrawalForm()→/api/member-info/withdrawal-info`(실제 point/couponCnt). 규칙 위반 + 중복. `withdrawal-info`는 레거시에 없던 이름(레거시는 `MemberInfoController`가 탈퇴 화면 렌더 시 model에 point/couponCnt 직접 addAttribute).
- **정합화(규칙: 기존 컨트롤러 데이터 → 페이지 모델 API로)**:
  - **백엔드**(`MemberInfoModelApiResource`): 빈 핑 `withdrawalFormEntry()` → **`withdrawalForm()`으로 교체**(레거시 point/couponCnt 로직 이식: `userService.getPoint` + `couponService.listBrandCouponCnt+listAllianceCouponCnt`). **`withdrawalInfo()`(`/api/member-info/withdrawal-info`) 삭제.** `mvn -o compile` BUILD SUCCESS.
  - **프론트**: `lib/member-info-withdrawal-api.ts` `getWithdrawalForm()` 경로 `withdrawal-info` → **`withdrawal-form`**. `page.tsx` 중복 `pingModel` 제거(미사용 import 정리) → **모델 API 1회 호출로 통일**.
  - 참조 조사: `withdrawal-info` 사용처는 프론트 1곳(이번 변경)·백엔드 정의 1곳뿐 → 안전 제거.
  - ⚠️ **배포 동시성**: 경로 변경이라 백엔드·프론트 **동시 배포** 필요(한쪽만 배포 시 탈퇴 화면 데이터 공백).

## 24. (08-04) /api 프록시 캐치올 통일 + 회원정보수정 점유인증 dev 버그 수정
- **버그**: dev에서 회원정보수정 진입 시 "인증번호 요청" 실패("서버와 통신할 수 없습니다"). 원인 = 클라이언트가 **Next 라우트 핸들러 경로**(`/api/member-info/sms/*`)를 호출하는데 **dev ELB 는 `/api/*` 를 백엔드로 직행** → 프론트 핸들러 우회 → 백엔드엔 그 경로 없음(실경로 `/api/sms/*`) → 404. (로컬은 핸들러가 받아 성공하던 비일관성)
- **방침 확정**: `/api/*` 는 **로컬/배포 모두 실제 백엔드 경로**로 통일. 개별 프록시 핸들러 폐기 → **로컬 전용 캐치올 프록시 1개**로 대체.
- **Phase 1 (캐치올 프록시)**:
  - 신규 **`app/api/[...path]/route.ts`** — 로컬(`pnpm dev`)의 모든 `/api/*` → dev 백엔드 전달 + Set-Cookie **Domain/Secure strip**(localhost 세션 유지) + `auth/logout` 요청쿠키 만료 특례. **배포 미동작 이중 보장**: ①ELB가 `/api/*`를 백엔드로 직행(프론트 미도달) ②`NODE_ENV!=="development" || APP_ENV∉{없음,local}` → 404 가드(배포 빌드=production 고정 → 항상 404, 죽은 코드).
  - **삭제**: `app/api/auth/[...path]`, `app/api/legacy/[...path]`(allowlist 방식) → 캐치올로 통합.
- **Phase 2 (경로 통일)**:
  - `lib/backend-api.ts` `backendApiUrl()` **환경분기 제거** → 항상 상대경로(로컬=캐치올·dev=ELB 백엔드).
  - **점유인증 경로 수정**: `ownership-confirm-flow.tsx` 의 `/api/member-info/sms/{send,check,verify}-ownership` → **`/api/sms/{...}`**(백엔드 실경로, JSON 호환).
  - **modify-info-form 쿠키 브리지 이관**: `app/api/member-info/modify-info-form`(우회 불가) → **`/page/member-info/modify-info-form-bridge`**(Node 핸들러, `/page/*`라 dev 도달). 클라이언트 `goModifyInfo` 를 hidden form POST(303→수정폼)로 변경. `MODIFY_INFO_AUTH_COOKIE` 동일 세팅. (SSR 검증 `lib/member-info-modify-api.ts`→백엔드 `/api/member-info/modify-info-form`(@RequestParam)는 실경로라 유지)
  - **삭제**: `app/api/member-info/sms/*`, `app/api/member-info/modify-info-form`(셰임).
- **Phase 3 (08-04 부분 완료)**: 셰임 개별 대조 후 **검증된 안전 항목만 삭제**.
  - ⚠️ **분석 에이전트(Explore)가 일부 클라 호출을 오분류**(card·check-name·chg-phone을 "클라 없음"으로) → blind 삭제 후 직접 grep 재검증으로 **위험 항목 복원**. 교훈: 셰임 삭제 전 클라 fetch/backendApiUrl 호출을 반드시 직접 확인.
  - **삭제 확정(안전)**: alliance/corporation, customer/{faq,notice-list,notice-view}, donation/intro, mypage/card/{password,reissue,reissue-status}, mypage/{my-point,donation-point}, store/{city,search}, member-info/{cert-identity,change-pw-form,withdrawal-form,confirm-pw-process,change-pw-process,modify-info-process,withdrawal-process}. 근거: (a)SSR 전용(클라 없음) 또는 (b)클라가 form/query로 백엔드 실경로+엔벨로프(api.model=ApiResponseWrapper 자동래핑) 일치 호출 → 캐치올/ELB 직행 정상. `store/search`만 클라 쿼리를 백엔드 실파라미터명 `q.metro/q.city/q.brandCode`로 수정.
  - **복원(삭제 시 회귀)**: `member-info/check-name`·`chg-phone`(클라 JSON `{authInfo}`→백엔드 `@RequestBody String` raw + 응답 raw Map, home.controller.rest 미래핑 → 핸들러가 변환 중), `card/*`×6(클라 호출+lib 변환 가능성+금전 민감).
  - **미완(개별 마이그레이션 필요, blind 삭제 불가)**: `event/list`·`event/my-coupon`(레거시 HTML 스크래핑→모델 API `/api/event/event-list` 응답 remap), `find-id-pw-process`·`find-id-pw-complete`(클라 JSON→`@RequestParam` form + 완료쿠키 브리지, dev 이미 깨짐), `check-name`·`chg-phone`(dev 이미 깨짐 — 클라를 raw string+rawMap 대응으로 수정 필요), `alliance/card`·`card/*`(dev 동작 확인 후 삭제 판단). → 각각 confirm-pw 방식의 개별 수정 대상.
  - 검증: `mvn` 무관, `tsc` 실제 소스 에러 0(25건은 삭제 라우트에 대한 stale `.next/dev/types` 스텁 → clean 빌드 시 소멸).
  - **순차 마이그레이션(08-04)**:
    - ✅ **check-name·chg-phone** (dev 깨짐 수정): `edit-form.tsx` 를 백엔드 실경로 직접호출로 이관 — 요청=authInfo **raw text**(`@RequestBody String`), 응답=**flat map**(`{code,authNm,authHp,authTelComCd,nameAuthInfo,data}`, `data`→encMnm) 직접 파싱. `/api/member-info/{check-name,chg-phone}` 핸들러 삭제. tsc 소스 clean.
    - ✅ **find-id-pw-process·complete** (dev 깨짐 수정): 단순 프록시가 아닌 **BFF 로직**(form 변환 + `_AUTH_INFO_TOKEN_` 서버사이드 쿠키 주입 + 결과/완료 쿠키 세팅)이라, 로직 보존한 채 **`/page/member-info/find-id-pw-{process,complete}-bridge`** 로 이관(dev ELB 우회 회피, 미들웨어 `-bridge` 예외 재사용). 클라(`find-id-pw-flow.tsx`·`find-id-pw-result-form.tsx`) 호출 URL 변경. `/api/member-info/find-id-pw-{process,complete}` 삭제. tsc 소스 clean.
    - 🔴 **event/list·my-coupon (미완, 별도 과제 권장)**: 현재 클라 `EventBrowser` 가 스크래핑 shim(`/api/event/list`→`{items:EventItem{seq,title,image,dday,category}}`, 레거시 `.spc` HTML 파싱)을 사용 — dev 에선 스크래핑 대상(`/page/*.spc`)도 백엔드 도달 불가라 이미 깨짐. 인터페이스(`EventListResponse{eventList:EventListItem{eventseq,title,category,sdate,edate,prizedate,bnPcPath,bnMobilePath,newyn,ddayCnt},categoryList,hasMore}`)는 정의돼 있으나, 모델 API `/api/event/event-list` 로 repoint 하려면 **클라 fetch+응답타입+카드 렌더링 필드 매핑까지 재작성**(image=bnPcPath/bnMobilePath, dday=ddayCnt, NEW=newyn, 기간=sdate~edate) → UI 회귀 위험. `search-index.ts` 도 `legacy-event` 사용 → 함께 검토. **스크래핑→모델 API 전환**은 UI 검증 포함 별도 집중 과제로 분리 권장.
- 검증: tsc(진행) + 로컬/dev 런타임(로그인 유지·점유인증 완주).

## 23. (08-04) 회원정보 허브 버튼 로그인상태 가드 alert
- **요청**: `/page/member-info/index` 에서 로그인 상태이면 아이디/비번찾기 클릭 시 "로그아웃 후 이용 가능합니다.", 미로그인 상태이면 회원정보수정/비번변경/회원탈퇴 클릭 시 "로그인 후 이용 가능합니다." alert(이동 차단). 기존 랜딩 화면은 유지.
- **구현(프론트 only)**: 신규 클라이언트 컴포넌트 `member-info/index/guarded-link.tsx`(`GuardedLink`) — `requireAuth: "in"|"out"` + `isLoggedIn` 로 `onClick` 에서 `preventDefault()`+`window.alert`. `page.tsx`: `getCurrentUser()`로 `isLoggedIn` 판정(+`force-dynamic`), 타일에 `requireAuth`(찾기=out, 수정/변경/탈퇴=in) 부여, `<a>`→`<GuardedLink>` 교체. 기존 스타일(`s.tile`/`s.withdrawLink`) 재사용. 백엔드 무관. tsc 신규에러 0.
  - **암호화 방식 최종 확정(08-03)**: **AES-256-GCM + 랜덤 IV 동봉** 확정. 검토·기각한 대안: ⓐ 고정 `sucToken.iv` 사용(GCM nonce 재사용=치명적) ⓑ CBC+고정IV+타임스탬프/난수프리픽스(위치·유일성·무결성 결여, HMAC 별도 필요) ⓒ 랜덤IV를 고정키+고정iv로 재암호화(IV는 비밀 불필요 → 이득0 + 고정IV 재도입 위험). 결론: IV는 공개가 표준(TLS/JWE 동일), 전체 base64 불투명 blob. nonce-misuse 근본회피 필요 시에만 AES-GCM-SIV(BouncyCastle/Tink) 별도 검토.
- **수신동의 강제 게이트 미사용 처리 → 즉시 원복(08-03, ha-web-api)**: 게이트 4곳(`AuthApiResource` 세션 세팅·`SpcInterceptor` 리다이렉트·`ReceptionAgreeController`/`ReceptionAgreeModelApiResource` 제거로직)을 주석 처리했다가 **사용자 요청으로 전부 원상 복구**(현재 활성 상태 유지). 세션 값 조사 참고: 로그인 관련 HttpSession 값은 `authToken`(SessionUser)·`global_enc_mbr_no`(=SHA512(mbrNo) 파생, SpcFilter 매요청)·`auth_return_url`·`PW_POPUP_OPENED`·`need_reception_agree`/`receptionRedirectUri`, 화면상태 `currentSiteType`/`_platform`, 플래시 `errors`/`successMessages`, 점유인증 `ownershipPath`. 무상태 전환 시 옮길 로그인 상태는 사실상 `authToken`(SessionUser) 하나(나머지는 파생/임시/요청스코프).

## 21. (07-31) 회원가입 약관(policy) 정합 — 동의값 POST 전달 + 약관 전문 이식 ★
- **Part 2 (기능)**: PolicyForm이 **앞단계에서 받은 값 + 화면 동의값을 전부 form POST**로 다음 단계 전달.
  - `AGREE_CODE` 매핑(레거시 pc/join/policy.jsp hidden name): 필수 `EUTL`(이용약관)·`ETDP`(제3자), 안내 `EIND=Y 고정`, 선택 `SIND/SBRD/STDP/SLOC`, 제휴사 `SBRD2~5`(신한/SK브로드/메리츠/SK엠앤). 멤플러스·광고성은 레거시처럼 **미제출**. 필수 체크 2개 = EUTL+ETDP.
  - PolicyForm props로 `authInfo(무조건)/reqPath/reqPage/reqChnl/joinInfo` 수신 → 히든필드로 POST. `router.push` 제거.
  - `policy/page.tsx`: 응답 `joinInfo` 읽어 props 전달. `middleware.ts`: **`POST /page/join/form` 브랜치** → policyInfo 전체 `hp_join_policy` 쿠키(303 GET). `join/form/page.tsx`: 쿠키→**`POST /api/join/form`**(백엔드 setPolicyCookie + `_AUTH_INFO_TOKEN_` 복호화 프리필), none-auth/error→공통 alert.
- **Part 1 (약관 전문)**: 프론트 모달 근사치 → 레거시 `popupTerms` 순수텍스트 **1:1 이식**.
  - 교체: `ETDP`(03)·`SIND`(04)·`SBRD`(07)·**`STDP`(05, ~40개 제휴사 전량**→`ALLIANCE_GROUPS` 온·오프라인 그룹핑)·제휴사 `SBRD2~5`(09~12)·멤플러스(08)·광고성(13/14). 제네릭 `affiliateTerm`/`AFFILIATES` 제거→업체별 실데이터.
  - 유지: `EUTL`(01, 399줄)·`SLOC`(06, 494줄)는 전체 법령문서라 **외부 링크 유지**. `notice`(02)는 이미 일치.
- **약관 대조 방법**: HTML 태그 제거 순수 텍스트로 비교 → `notice`(EIND)만 기존 일치, 나머지는 프론트 임의요약이었음(제3자=SPC 계열사 실명 누락 등) 확인 후 교체.
- tsc: 신규 에러 0(baseline 17 유지).
- 후속(미결): `join/form` 프리필(RSLT_NAME/TEL_NO/isUnder14/joinInfoObj)을 JoinInfoForm에 연결(현재 폼 props 미수신).

- `pingModel()` 서버 로그의 페이지 파라미터를 원본 query/body 문자열 대신 JSON 객체로 직렬화하도록 변경했다. 빈 값은 `param={}`, 예: `?redirectUrl=HCHP&cd=HPWW`는 `param={"redirectUrl":"HCHP","cd":"HPWW"}`로 기록하며 중복 키는 배열로 보존한다. `model=`은 기존과 같이 API 응답 JSON을 기록한다. 전체 `tsc` 및 eslint 실행은 패키지 프로세스가 시간 제한을 넘어 완료 결과를 얻지 못했다.

- `/page/join/policy`는 `pingModel()` 대신 직접 `POST /api/join/policy`를 호출하므로 별도 디버그 로그를 추가했다. local/dev/stg(`APP_ENV !== "prod"`)에서만 `param={authInfo,reqPath,reqPage,reqChnl}`과 `model={...}`을 출력하며 운영에는 인증 정보가 기록되지 않는다.

## 다음 할 일 (TODO)
- [ ] ⚠️ (08-07, 인프라문의) **[운영 Scouter APM 오브젝트 미표시 — 수집기 6100 인바운드 차단]** 운영 톰캣(web-tomcat-10.1.57, `/app/server/`)에 Scouter 에이전트는 **정상 로딩**(2.21.3 JDK21호환, `-javaagent` 적용, `objName:/ip-11-7-12-91/WEB` 등록, Counter Collector Started — catalina.out 확인)됐으나 수집기 화면에 오브젝트 미표시. **원인 확정**: app서버(11.7.12.91·11.7.62.171)→수집기(11.7.11.31) **TCP 6100 = TIMEOUT(양쪽 동일)** = 방화벽/보안그룹 silent drop(refused 아님=미기동 아님). **조치(인프라팀 문의)**: 수집기 11.7.11.31 인바운드에 **TCP 6100 + UDP 6100** 을 app서버(11.7.12.0/24·11.7.62.0/24 또는 app SG)에서 허용 + 서브넷 NACL + 호스트 방화벽(firewalld) 확인. (Scouter는 TCP=등록/XLog, UDP=alive/카운터 **둘 다** 필요.) 톰캣/에이전트/scouter.conf(collector=11.7.11.31, obj_name=WEB→호스트접두사로 01/02 구분) 설정은 문제없음. 기동은 **반드시 `bin/startup.sh`**(javaagent 주입). **→ SG 오픈 후 app서버 `nc -vz 11.7.11.31 6100` succeeded 확인 → 오브젝트 표시(재기동 불요, 에이전트 자동 재연결).**
- [~] (08-07, ⏳배포대기) **[로그인 Cloudflare Turnstile 캡차 — 프론트 위젯 + 백엔드 서버검증(우회불가)]** 레거시 `sso/login.jsp`(위젯)+`sso/checkauth.jsp`(siteverify 서버검증) 정합. web2 로그인은 클라가 `/api/auth/login` 직접 POST(배포 시 ELB→백엔드 직행)라 **프론트 위젯만으론 우회 가능** → 백엔드 검증 필수. 구현(프론트3+백엔드4): **백엔드(j-ha-web-api)** ①`LoginVm` `captchaToken` 필드 ②`application.yml` `cloudflare.site-key`(공개)+`secret-key`(env `${CLOUDFLARE_TURNSTILE_SECRET:}` 주입, 기본값=레거시 checkauth.jsp 값) ③`AuthModelApiResource`(GET /api/auth/login) 가 `{turnstileSiteKey}` 반환(공개키만, 시크릿 절대 미반환) ④`AuthApiResource.login()` 최상단 `verifyTurnstile()`(java.net.http + Jackson, siteverify POST secret+response+remoteip) → `success!=true`/시크릿미설정/통신오류 전부 **fail-closed 로그인차단**(`{success:false, message:"캡차 인증이 확인되지 않았습니다"}`). **프론트(web2)** ⑤`login/page.tsx` 모델API(`legacyContractGet`)에서 siteKey 획득→`<LoginForm siteKey>` ⑥`login-form.tsx` placeholder→Turnstile 위젯(explicit render, api.js), 토큰 없으면 로그인버튼 비활성, POST 바디 `captchaToken`, 실패 시 `turnstile.reset()`, siteKey 없으면 캡차오류 안내+차단 ⑦`next.config.mjs` CSP script-src+frame-src `https://challenges.cloudflare.com`. `login.module.css` `.captcha`/`.captchaError` 추가. **검증**: 백엔드 `mvn compile` BUILD SUCCESS, 프론트 tsc 로그인 에러 0. **공개 사이트키만 프론트로, 시크릿은 서버검증 전용.** **→ 배포(프론트+백엔드 동시) 후: 캡차 미통과 시 로그인 불가 + 위젯 직접우회(토큰없이 POST) 시 백엔드 차단 확인. ⚠️ 배포 전 `CLOUDFLARE_TURNSTILE_SECRET` env 설정 권장(미설정 시 yml 기본값=레거시값으로 동작).**
- [~] (08-07, 최종감사) **[파바앱+휴면 4흐름 독립 병렬 검증 + 치명버그 수정]** 병렬 검증 에이전트 4개(HMOI 정보수정 / 비번변경·탈퇴·쿠키 / 아이디비번찾기 / 일반 휴면)로 web2↔백엔드모델API↔레거시 대조. **적용한 수정 3건**: ①🔴**[치명]** 미들웨어 `_EXOG_MEMBER_INFO_INSAUT_` 쿠키 path `/page/brand/member`→**`/`** (백엔드 `setBrandMemberCookie(INSAUT,...,"/")` 와 일치). 좁은 path면 브라우저가 `/api/sms/*`(점유인증 send/check)·`/api/brand/member/modify-info-complete`(수정제출) **클라이언트 fetch 에 쿠키 미첨부 → H499 → 파바앱 실기기 100% 실패**였음. path=/ 로 전역 첨부. (CONFIRMED 쿠키는 그대로 /page/brand/member) ②고아+유출 페이지 `brand/member/confirm-complete/` 삭제 — confirm-view 는 이제 confirm-complete-bridge 로 POST 하는데 이 구페이지는 **pw 를 GET searchParams 로 읽어(URL 유출)** + `res.result.redirectTo`(백엔드 미반환) 참조로 항상 H952 폴백하는 죽은 경로였음. ③check-ownership 에 `reqPath` 추가(레거시 정합, HIST.authChnl OPBS 로깅). `tsc` 내 변경파일 에러 0. **→ 배포 후 실기기 재검증.**
  - **✅ [수정완료·⏳배포대기] HWDR(탈퇴)/HCHP(비번변경) confirm→target `data` 핸드오프 재배선**: (원인) 백엔드 `withdrawalView`/`modifyPwView` 는 **`data`**(=base64(base64("yyyyMMddHHmmss||pw")), 10초유효)로 pw 재검증(`verifyPw`, Resource:121-145)하는데, 이전 세션이 confirm-view→`confirm-complete-bridge`→`confirmComplete`(CONFIRMED 쿠키만)→target 으로 **`data` 없이** 보내 **HWDR 크래시**(confirmRedirect 응답에 point/couponCnt 없음 → `couponCnt.toLocaleString()`)·**HCHP 게이트 스킵**이었음. `_EXOG_MEMBER_CONFIRMED_` 는 백엔드가 읽지 않음(레거시에도 이 단계 없음). (레거시 정본 `opbs/confirm-view.jsp:132-137`) pw→`data=btoa(btoa("ts||pw"))`를 **target 으로 직접 POST**. **수정(레거시 정합, HMOI 브리지 패턴 미러)**: ①`confirm-view/brand-confirm-pw-form.tsx`(신규 client) — pw→`data` 생성(레거시 `fn_convertDateStr` 동일 yyyyMMddHHmmss + 이중 btoa)→`confirm-pw-bridge` POST. ②`confirm-view/page.tsx` HCHP/HWDR 분기 → `<BrandConfirmPwForm>`. ③`confirm-pw-bridge/route.ts`(신규) — `data`로 withdrawalView/modifyPwView **1회 호출**(pw 재검증·point/coupon 조회) → 성공(landingType `withdrawal-view`/`modify-pw-view`) 시 결과를 `hp_brand_confirm_pw_form` 쿠키(target 경로한정, 120s)에 담고 303 → target. 실패(verifyPw→confirmRedirect `cd` HPWL/HPWW) → `confirm-view?redirectUrl&cd` 재유도. ④`withdrawal-view/page.tsx`·`modify-pw-view/page.tsx` — searchParams `data` POST 제거, **쿠키만 읽어** 렌더(없으면 confirm-view 재유도). ⑤죽은 `confirm-complete-bridge/` + `lib/brand-member-confirm-complete-api.ts` + `interface/.../confirm-complete.ts` **삭제**. `lib/brand-member-cookies.ts`에 `BRAND_CONFIRM_PW_FORM_COOKIE` 추가. `tsc` 신규/변경 에러 0. front-only. **→ 배포 후 파바앱 실기기: 탈퇴/비번변경 진입→비밀번호 확인→탈퇴안내(포인트/쿠폰)·변경폼→완료 확인. 비번오류 시 HPWL/HPWW 안내.**
  - 기타 경미(미수정): HMOI OTP 타이머 useEffect deps `[phase]`(일반 OwnershipConfirmFlow 와 동일 패턴, 동작 정상) · 찾기 응답 Set-Cookie 역전파 미지원(legacy-http, 기능무해·replay 창) · 휴면 onlnId 공백 회원 ID입력 UI 부재(엣지) · 휴면 14세미만 보호자정보 미지원(레거시 PC 동등) · reqChnl 로그 공란.
- [~] (08-07, ⏳배포대기) **[파바앱(OPBS) 회원정보수정(HMOI) SMS 점유인증 + 편집폼 전체 이식]** 확인 결과 브랜드 정보수정은 앱 최대 플로우(일반 WEB `OwnershipConfirmFlow`→`modify-info-form-bridge`→`modify-info-form`→`edit-form`)의 **브랜드판 전체 미구현(stub)**: confirm-view HMOI 가 native POST 버튼 1개(OTP 입력 없음), 브리지 없음, modify-info-view 는 readonly 표시+GET 저장뿐이었음. **레거시 opbs `confirm-ownership-view.jsp` 정합**으로 이식 — 진입 즉시 `/api/sms/send-ownership` 자동발송 → OTP 6자리 → `/api/sms/check-ownership` → sucToken → 수정폼. **백엔드 분석 핵심**: ①`send-ownership` 은 **reqPath 비공백이면 `_EXOG_MEMBER_INFO_INSAUT_` 브랜드쿠키 로그인**(getBrandLoginCheck), 없으면 일반 세션 → 브랜드는 **reqPath 필수**. ②`check-ownership` 은 reqId+authNo 만(로그인 무관) → 그대로 동작. ③`verify-ownership`(KCB 폴백)의 PROFILE 은 `SecurityUtils.getCurrentUser()`(일반 세션)만 지원 → **파바앱(세션 없음) 미지원** → KCB 폴백 대신 **안내(창닫기)** 로 대체(백엔드 제약). ④`modify-info-view`(POST token=sucToken) = 프리필(로그인+fn_verifySucToken+checkMemberOwnershipHist(nonce)+getMemberDetail), `modify-info-complete`(@RequestParam Map) = 로그인쿠키+userId일치+encMnm+modifyInfo. **신규/수정 6파일**: `lib/brand-member-cookies.ts`(FORM 쿠키 상수), `confirm-view/brand-ownership-flow.tsx`(신규, SMS 점유인증 클라 — 타이머·모달, sucToken→bridge POST), `confirm-view/page.tsx`(HMOI 분기 native POST→`<BrandOwnershipFlow>`), `modify-info-view-bridge/route.ts`(신규, sucToken 1회소비→memberDetail 슬림 FORM쿠키→303, 일반 bridge 패턴 미러), `modify-info-view/page.tsx`(FORM쿠키 소비→편집폼, 없으면 confirm-view HMOI 재유도), `modify-info-view/brand-edit-form.tsx`(신규, 이메일 중복확인+Daum 주소+마케팅동의 → `backendApiUrl(/api/brand/member/modify-info-complete)` form → 성공 모달→`closeAppWebview()`). 이름/휴대폰 변경은 KCB(세션) 필요 → 파바앱 미지원이라 읽기전용. `tsc` 신규파일 에러 0. front-only(백엔드 무변경). **→ 배포 후 파바앱 실기기: 회원정보수정 진입 즉시 SMS 발송 → OTP 입력 → 편집폼(이메일/주소/마케팅) → 수정하기 → 완료 창닫힘 확인. (⚠️ KCB 폴백 미지원은 백엔드 verify-ownership 브랜드 세션 지원 시 후속.)**
- [x] (07-31→08-03 완료) **회원가입 전 구간 dev E2E 완주**(본인인증→약관→정보입력→가입경로→joinProc→welcome). 프리필/none-auth/authInfo쿠키/데이터컨트롤/dev라우팅(§16~21) 전부 해결. **남은 실검증**: 아이핀 회원·14세 미만 경로.
- [x] (08-03 **dev 완주 성공**) **회원탈퇴 dev 라우팅 이슈 수정** — 원인: 백엔드 `confirm-pw-process`가 `@RequestParam(form)`인데 프론트 JSON 전송. 로컬은 Next route handler(`app/api/member-info/*`)가 변환/정규화했으나 **dev는 ELB가 `/api/*`를 백엔드로 직접 보내 route handler 우회 → 비번확인 실패**. 수정: confirm-form = `backendApiUrl`+form+`code==00` 판정, withdrawal-form의 `_MEMBER_CONFIRMED_` 가드 제거, `/api/legacy` 허용목록에 process 4종 추가. **dev 재배포 후 비밀번호 확인→탈퇴 유의사항→동의→탈퇴 완주 확인 완료.**
- [~] (08-03, **dev 미확인**) **member-info 전 플로우 dev 라우팅 정합** — 백엔드 process API가 전부 `@RequestParam(form)`인데 프론트 JSON + Next route handler(정규화/쿠키) 의존 → dev ELB 우회로 깨짐. confirm-pw(HWDR/HCHP 통합)·change-pw·withdrawal·modify-info process 호출을 **form+backendApiUrl**로 통일, change-pw 게이트를 **`_MEMBER_CONFIRMED_` 쿠키** 기반으로 전환, `/api/legacy` 허용목록 4종 추가. **미결**: modify-info 진입(ownership SMS + MODIFY_INFO_AUTH_COOKIE 게이트)은 여전히 route handler 의존 → 별도 수정 필요. change-pw 응답 shape dev 확인 필요. **전반 dev 재배포 확인 미완.**
- [x] (08-03 완료, 로컬 기준) **회원탈퇴 흐름 레거시 정합** — 순서가 반대(유의사항→비번)였고 비번확인 성공 시 즉시 탈퇴되던 것을 **비번확인 → 탈퇴 유의사항/동의 화면 → "탈퇴"(동의) 버튼 → withdrawal-process**로 수정. Hub 링크→`confirm-pw-form?redirectUrl=HWDR`, `confirm-form`(HWDR) 즉시탈퇴 제거→`withdrawal-form` 이동, `withdrawal-form`에 `_MEMBER_CONFIRMED_=HWDR` 가드 + 신규 `WithdrawButton`(confirm→withdrawal-process→로그아웃→완료모달). **모델API 정정**: 탈퇴 안내를 `/api/mypage/my-point`(쿠폰=0) → **`/api/member-info/withdrawal-info`**(포인트+쿠폰수)로 교체. confirm-form 기존 tsc 2건도 수정.
- [ ] (08-03) ★ **[2단계] 로그인 완전 무상태화(세션 제거, 쿠키만)** — 1단계(암호화 쿠키+세션 부트스트랩) 운영 안착 후. `SecurityUtils.getCurrentUser()`/`isAuthenticated()` 구현부를 세션 대신 **쿠키(요청스코프)** 기반으로 교체(Java 호출부 141곳 중 대다수 자동 커버) + `getSession().getAttribute(authToken)` 직접 접근/**JSP 세션 직접참조** 소수 지점 개별 수정 → `HttpSession` 로그인 유저 제거로 **인스턴스 힙 메모리 절감**. + 2단계 표준화(JWT/JWE 서명검증·refresh·키회전) 함께 검토. **선행 조사**: 직접 세션접근 지점(특히 JSP) 실수량 정밀 카운트.
- [x] (08-05 완료) ★ **[인프라 검증] ALB 스티키 해제 → 라운드로빈 전환** — `_HPC_AUT` 무상태 인증 기반으로 백엔드/프론트 TG 스티키 OFF(라운드로빈) 전환·검증 완료. 세션 복원·로그인 유지 정상.
- [x] (08-04) **[회원정보수정 점유인증 레이스 — nonce 상관키로 수정 완료]** 증상: 회원정보수정 인증번호 검증 성공 후 modify-info-form 진입 시 "빠르면 실패/느리면 통과". **원인**: 점유인증 이력(`MEMBER_OWNERSHIP_HIST`) 조회가 `APP_UUID = HttpSession.getId()`(JSESSIONID) 기준 → 프론트·백엔드 **둘 다 라운드로빈(세션 스티키/복제 없음)** 이라 검증요청(INSERT)과 폼진입요청(SELECT)의 세션ID 불일치 → `checkMemberOwnershipHist` miss → `"4need-ownership"`. (세션 전파 지연이 "빠르면 실패"로 나타남. `IV`는 base.iv 고정이라 부적합, `_HPC_AUT` IV는 슬라이딩 재발급 시 회전해 부적합.) **해결**: sucToken 평문에 이미 담겨 왕복하고 HIST.NONCE에도 이미 적재되는 **nonce**를 조회 상관키로 사용(세션 비의존). **변경 파일**: `SmsService`(fn_getNonceFromToken 헬퍼 추가 + checkMemberOwnershipHist 파라미터 sessionId→nonce), `SmsRepository.xml`(checkMemberOwnershipHist·updateMemberOwnershipHistConfirmDt: `APP_UUID`→`NONCE`), 호출부 4곳(`MemberInfoModelApiResource`·`BrandMemberModelApiResource`·레거시 `MemberInfoController`·`BrandMemberController`). INSERT의 APP_UUID 컬럼은 그대로 유지(무해). **✅ 스테이징 배포·테스트 완료 — 인증번호 빠르게 입력해도 실패 없음(레이스 해소 확인, 2026-08-04).**
- [x] (08-04) **[로그인 슬라이딩 renewal을 미들웨어로 이관 — 브라우저 전달 배관 수정]** 문제: `_HPC_AUT` 재발급/만료 Set-Cookie가 `/api/auth/check`(=`getCurrentUser`) 응답으로 오지만, 그게 **RSC 렌더 컨텍스트**라 브라우저로 relay 불가(RSC는 쿠키 쓰기 불가) → 순수 페이지 이동만 하는 사용자는 슬라이딩 연장/만료삭제가 브라우저에 반영 안 됨. 해결: **미들웨어(`middleware.ts`)에서 `/api/auth/check` 1회 호출** → ①재발급/만료 `_HPC_AUT` Set-Cookie를 응답에 relay(`applyAuthCookies`, dev는 Secure/Domain strip) ②로그인 결과를 `x-hp-user` 헤더로 RSC 전달. `getCurrentUser`는 `x-hp-user` 우선 읽고 없을 때만 직접 check(안전망) → **중복 호출 없음(페이지당 1회 유지)**. 보안: `x-hp-user` 유입값(클라 위조) 미들웨어에서 제거 + 렌더 경로는 항상 덮어씀. getCurrentUser 호출부는 전부 RSC 페이지뿐(‑ /api 없음)이라 위조 벡터 차단. 변경: `middleware.ts`(resolveAuth/applyAuthCookies/x-hp-user), `lib/auth-server.ts`(헤더 우선 리더). **→ 스테이징 배포 후 "순수 페이지 이동 3분 전/후 `_HPC_AUT` 값 변화" + "60분 무활동 후 쿠키 삭제·로그아웃" 테스트 필요.**
- [x] (08-04) **[로그인 만료정책 — 슬라이딩 게이트 30분→3분 수정 완료]** 문제: `_HPC_AUT` 재발급이 만료 절반(30분) 경과 시에만 일어나 **첫 30분 내 활동이 세션 연장에 미반영**(예: 1:00 로그인·1:29까지 활동 후 유휴 → 2:05 만료 로그아웃). 해결: `AuthCookieService.needsSlidingRefresh` 게이트를 **고정 3분**(`SLIDING_REFRESH_MILLIS`)으로 낮춤 + `Math.min(3분, maxAge/2)` 방어 상한. → 실효 만료 = **마지막 활동 + 60분(오차 ≤3분)** 롤링. 재발급 시 `issue()`가 timestamp=now 갱신 + 쿠키 maxAge 60분 리셋(서버·브라우저 양쪽). 비용: 활동 중 최대 3분당 1회 재암호화(무시 수준). **→ nonce 배포·테스트 완료 후 별도 커밋으로 배포 예정.**
- [~] (08-05) **[파바앱(OPBS) 회원가입 URL 재설계 + PC차단 alert]** 진행중. **완료·동작**: ①URL 이동 `/page/brand/member/{join/index,policy,join-view,join-optional,join-complete}`→`/page/brand/join/{index,policy,form,optional-form,welcome}` + 모델API `/api/brand/member/join-*`→`/api/brand/join/*`(백엔드 `BrandMemberModelApiResource` @Mapping 5개, join-gate 제외) + 전 참조/미들웨어 OPBS 목적지·join-gate 링크 갱신(유입 rewrite 소스 `/page/join/auth.spc?instCd=OPBS` 불변). ②**브랜드 alert 페이지 신설** `/page/brand/common/alert`(alertType=invalid-access="잘못된 접근입니다."+[확인]→`window.close()`, 레거시 `opbs/comm-complete.jsp` `page.cancel` 이식) + 본인인증 page가 PC(code="PC")/landing=false 시 alert로 redirect. ③**스캐폴딩**: `PolicyForm` 재사용화(`actionPath`/`instCd` prop, 일반 무영향) + 브랜드 브리지 3종 신설(`policy/form/optional-form-bridge`). **배선 완료(Phase 2)**: join-auth form action→`/page/brand/join/policy-bridge`, 미들웨어 브랜드-policy POST 핸들러 **제거**, 브랜드 `policy/page.tsx`→**`<PolicyForm actionPath="/page/brand/join/form-bridge" instCd>`**(일반 약관 공유), `form`·`optional-form` 페이지를 **searchParams→브리지 쿠키(`hpw_brand_join_form`/`_optional_form`)** 기반으로 전환(민감값 URL 노출 제거), form→optional 전환도 GET링크→**form POST(optional-form-bridge)**. `PolicyForm`에 `actionPath`/`instCd` prop 추가(일반 무영향). interface 파일도 `interface/page/brand/join/{index,policy,form,optional-form,welcome}.ts`로 이동+import 정합. **⚠️ 미검증**: 전체 회원가입 플로우(특히 _AUTH_INFO_TOKEN_ 쿠키가 policy→PolicyForm→form-bridge→backend 로 전달되는 인증 연속성)는 **빌드+실기기(모바일 UA) 테스트 필수**. 백엔드 모델API↔레거시 로직 5/5 정합 확인됨. **(08-05) 브랜드 form/optional placeholder → 실폼 완성**: 감사 결과 브랜드 ③정보입력·④부가정보가 placeholder(빈 input)이고 생성(joinProc) 미배선이었음 → 사용자 확인(약관/화면/플로우 전부 일반과 동일, instCd만 다름) → **일반 컴포넌트 공유화**: `join-info-form.tsx`(+`actionPath`/`instCd`)·`referral-form.tsx`(+`joinProcPath`/`welcomePath`/`instCd`) prop 추가(일반 기본값=무영향), 브랜드 `form`·`optional-form` 페이지를 일반 페이지 미러(같은 ctx·프리필·policyCookie 캡처)로 재작성해 **동일 `<JoinInfoForm>`·`<ReferralForm>` 렌더**. → 브랜드도 실입력(아이디/비번/이메일/주소/통신사) + `/api/brand/join/joinProc` 최종 생성까지 완성. 일반 회원가입 회귀 무영향(기본 prop 유지).
- [x] (08-05) **[파바앱 회원가입 채널코드 A005/A007 버그 수정]** 증상: 파바앱(reqPath="OPBS") 회원가입 완료(2100 전문) 시 **모바일앱(A005/30)이 아니라 모바일웹(A007/31)로 전송**됨. 원인: `WebUtil.setHeaderBrandAndChnlByReqPath`가 `"APP"→A005/30`·`"ORDER"→A009/30`만 처리하고 **`"OPBS"` 누락** → 헤더가 A005/30·A009/30이 아니면 `ApiService.buildHeader` 게이트가 `A007 + WebUtil.isMobile()?31:20`으로 리셋(파바앱 웹뷰=모바일 → 31). 채널 5분류: pc=A007/20·mo=A007/31·APP=A005/30·ORDER=A009/30·**OPBS=A005/30(=해피앱 동일, `JoinService:418` 주석)**. **설계 확정(사용자)**: **reqPath 에는 instCd 를 넣는다 + instCd(=reqPath) 존재하면 무조건 A005/30**(당장 OPBS만, 향후 OUNF 등은 instCd별 분기 여지). **수정**: ①`WebUtil.setHeaderBrandAndChnlByReqPath` — APP/ORDER 외 **reqPath 비어있지 않으면(=브랜드 instCd) A005/30** (도메인: ""|APP|ORDER|브랜드instCd, 향후 instCd별 분기 지점 주석). ②프론트 `brand/join/index/join-auth.tsx` — reqPath 하드코딩 "OPBS" → **`instCd`(동적)**. 레거시 참고: 레거시는 파바앱 **가입만** reqPath="APP" 강제(→A005/30), 수정 등은 OPBS→A007/31 였으나 → **본 수정은 사용자 지시대로 브랜드 전 플로우 A005/30 통일**. 가입완료 API는 일반(`POST /api/join/joinProc`, JoinResource)·브랜드(`POST /api/brand/join/joinProc`, BrandMemberResource) **분리**돼 있고 둘 다 공유 `saveNewMember`→동일 `setHeaderBrandAndChnlByReqPath` 사용. **(08-05) 브랜드 생성 API URL 통일**: `/api/brand/member/join-proc` → **`/api/brand/join/joinProc`** (BrandMemberResource 클래스 base `@RequestMapping("/api/brand/member")` 제거 + 각 메서드 full path 부여 → join-proc만 신규 경로로, check-id/check-email/change-pw/check-name/chg-phone은 `/api/brand/member/*` 유지). 프론트는 아직 브랜드 join-proc 미호출(placeholder) → 배선 시 신규 URL 사용. 전송경로: `saveNewMember`(:167 변환, :332 `apiService.send`) → `ApiService.buildHeader`(A005/30 보존). **→ 백엔드 rebuild 후 파바앱 가입 시 2100 헤더 A005/30 확인.**
- [x] (08-05) **[파바앱 복귀 버튼 — 웹 홈 이동 제거, 레거시 goHome 이식]** 파바앱 웹뷰는 웹 홈/앱메인으로 이동하면 안 됨. 레거시 `opbs/join-complete.jsp` `goHome()`(urlMap POST 복귀 / window.close 폴백) 이식. **신설** `app/(site)/page/brand/brand-return-button.tsx`(client): urlMap(`BRANDAPP_ATTR.ATTR_VAL`, 앱 복귀 URL) 있으면 hidden form POST(code/message/id)→앱 복귀, 실패/없음→`window.close()`, **웹 홈 이동 없음**. 적용: ①**welcome** — `/api/brand/join/welcome` urlMap(0001)로 `<BrandReturnButton label="해피포인트 시작하기">`(기존 returnLinks Cta 제거). ②**alert** — 진입 redirect에 `instCd` 추가 + `GET /api/brand/member/brand-attr?instCd&attrCd=0001`로 urlMap 조회 → `<BrandReturnButton>`(기존 window.close-only → 앱 복귀 포함). ③**error.tsx 5개** — [다시시도](reset)→[확인]`window.close()`. **레이아웃/헤더/브레드크럼(홈 링크)은 미변경**(사용자 지시 — 기능 확인 후 별도 요청). urlMap 조회 = 브랜드 전용(일반 회원가입은 미조회).
- [~] (08-05) **[일반 회원가입 완료 → Airbridge 트래킹 링크(해피포인트앱 랜딩)]** 일반 `/page/join/welcome` "해피포인트 앱으로 이동하기" 버튼에 Airbridge `createTrackingLink` 적용(파바앱 제외). 신설 `airbridge-app-button.tsx`(client): `airbridge.init({app,webToken})` + `createTrackingLink('ha-web', {campaign:'ha-web-sign-up', deeplink_url:napi event-view.spc?eventseq=fbdf..., fallback_ios/android:'store', fallback_desktop:동일URL}, ({shortURL})=>...)` → 버튼 클릭 시 shortURL 이동. 준비 전/토큰 미설정/실패 시 **스토어 폴백**(그레이스풀). welcome 에서 `AppOpenCta`→`AirbridgeAppButton` 교체. deps에 **`airbridge-web-sdk-loader@^1.2.1`** 추가(⚠️ **`pnpm install`로 lockfile 갱신 필요** — sanitize-html 교훈). env `NEXT_PUBLIC_AIRBRIDGE_APP`·`NEXT_PUBLIC_AIRBRIDGE_WEB_TOKEN` 자리 추가(값 대기). **미확정(기본값 사용)**: deeplink_url을 준 web URL 그대로(스킴 감쌈 여부 미정)·fallback_desktop=동일URL. **대기**: app/webToken 발급값, deeplink_url 형태(web vs happypointcard:// 스킴) 확인. **⚠️ (08-05) 배포 실패 → 완전 제거(롤백)**: `airbridge-web-sdk-loader` dep 을 package.json 에 추가했으나 `pnpm-lock.yaml` 미갱신 → 스테이징 배포(`pnpm install --frozen-lockfile`)가 **~35초만에 FAILURE**(delegate `#76`). → 사용자 지시로 **airbridge 전부 제거**(dep·`airbridge-app-button.tsx` 파일 삭제·welcome 원래 `AppOpenCta` 복원·`.env` `NEXT_PUBLIC_AIRBRIDGE_*` 삭제, `grep -i airbridge` 0건). 워킹트리는 **파바앱 수정본까지만**. **재적용 교훈**: 새 dep 추가 시 반드시 `pnpm install` 로 lockfile 갱신·커밋 후 배포(frozen-lockfile). 확정값(channel=ha-web·campaign=ha-web-sign-up·deeplink_url=napi event-view.spc?eventseq=fbdf...·fallback store)은 이 워크로그에 보존.
- [x] (08-05) **[SSR 계약 API가 브라우저 User-Agent 미전달 → 모바일이 PC로 오판(파바앱 PC차단 오작동)]** 증상: 폰으로 `/page/join/auth.spc?instCd=OPBS` 접속했는데 백엔드가 "모바일 환경 아님"(PC 차단). 원인: `lib/legacy-http.ts`의 `legacyGet/legacyPost` 헤더에 **User-Agent 없음** → SSR fetch가 백엔드에 폰 UA를 안 넘겨 `WebUtil.deviceType`(UA 기반)이 PC로 판정. 해결: **UA를 쿠키처럼 포워딩** — `readRequestUserAgent()`(next/headers `user-agent`) 신설 + `legacyContractGet/Post`가 원요청 UA 읽어 `legacyGet/Post` 헤더로 전달(`if (opts.userAgent) headers["user-agent"]=...`). → 모든 계약 API가 실제 브라우저 UA를 백엔드에 전달(device 판별 정확). 파바앱 PC차단은 이제 실제 PC에서만. **→ 빌드/배포 후 폰으로 `/page/join/auth.spc?instCd=OPBS` 재확인.**
- [~] (08-05, ⏳배포대기) **[파바앱 본인인증 페이지 — 일반과 레이아웃 통일 + 아이핀 복원]** 증상: 파바앱 본인인증 페이지 상단 타이틀/레이아웃이 일반 회원가입과 다름(타이틀 "본인인증", Section 래핑, 브레드크럼 홈>회원>본인인증, 휴대폰만). 사용자 지시 "파라미터 형태만 다르지 레이아웃은 다 같아야". **수정** `app/(site)/page/brand/join/index/page.tsx`: ①PageHero 타이틀 "본인인증"→**"해피포인트 생활 첫단계"**·lead 일반과 동일 ②브레드크럼 홈>회원>본인인증 → **홈>회원가입** ③`<Section>` 래핑 제거(일반은 Subpage 직속) ④metadata title "본인인증"→"회원가입" ⑤**아이핀(I-PIN) 인증수단 복원**(휴대폰+아이핀 2종, 일반과 동일 아이콘/문구). 아이핀은 브랜드 `join-auth.tsx`가 이미 `openCert("ipin")` 처리 → 추가 배선 없이 동작. 유지된 차이(의도): instCd 전달·PC차단(getJoinAuth landing=false→브랜드 alert)·cert reqChnl=instCd·약관 POST 목적지 `/page/brand/join/policy-bridge`. **→ 배포 후 실기기 확인 필요.**
- [x] (08-05, ✅제거완료) **[임시 디버그] 실제 유입 URL 확인 alert** — 사용자 질의 "파바앱이 진짜 랜딩시킨 URL 모르겠다(로그엔 rewrite 후 경로만 찍힘)". rewrite 는 주소창을 안 바꾸므로 `window.location.href`가 진짜 유입 URL. **(08-05 위치 변경)** 파바앱이 **일반 회원가입으로 유입되는 정황** 확인 → 디버그 alert 를 `brand/join/index/join-auth.tsx`에서 **제거**하고 **`join/index/join-auth.tsx`(일반)로 이동**. 진입 시 `window.alert`로 href/pathname/search/referrer/reqPath 표시. **확인 완료 후 이 블록 제거 예정.**
- [~] (08-05, ⏳배포대기) **[파바앱 유입 rewrite — /page/join/index.spc?reqPath=OPBS → 브랜드 가로채기]** 증상: 파바앱 회원가입이 **일반 회원가입으로 감**. 원인: 파바앱 실유입 경로가 `/page/join/index.spc?reqPath=OPBS`(디버그 alert 로 확인)인데, ① `OPBS_REDIRECT_MAP`엔 `/page/join/auth.spc`만 있고 **`/page/join/index.spc` 누락** → 아래 (0-a) 일반 통합 rewrite 로 흘러 `/page/join/index`(일반)로 감. ② OPBS 감지 조건이 `instCd===OPBS`만 봐서 **`reqPath=OPBS` 유입을 놓침**. 수정 `middleware.ts`: ①`OPBS_REDIRECT_MAP`에 `"/page/join/index.spc":"/page/brand/join/index"` 추가 ②OPBS 감지를 `instCd===OPBS || reqPath===OPBS`로 확장 ③reqPath=OPBS로만 온 경우 브랜드 페이지가 읽을 `instCd`가 없으므로 rewrite URL 에 **`instCd=OPBS` 보강**. OPBS 분기는 일반 통합 rewrite보다 먼저 실행되므로 브랜드로 우선 가로챔. **유입 rewrite 불변 규칙 준수**(기존 경로 유지, 추가만). **⚠️ (08-05 실기기 재확인)**: dev 파바앱 유입 = `dev-www.../page/join/index.spc` **파라미터 전무**(host 이미 dev-www → dev→dev-www 리다이렉트 무관, serverSearchParams={}). UA 는 일반 웹뷰(`wv`)라 구분 불가. **✅ (08-05 운영 스카우터 로그로 원인·해결 확정)**: 운영(www)에서 파바앱은 `/page/join/auth.spc?instCd=OPBS` 로 오며, **`x-requested-with=com.pb.android` 헤더**(파리바게뜨 앱 패키지명)를 항상 실어보낸다(+쿠키 `_ga4_reqPath=pbapp`·`_EXOG_MEMBER_INFO_INSAUT_=OPBS|`). → **파라미터 없어도 이 헤더로 서버에서 파바앱 확정 가능**. **해결**: `middleware.ts` OPBS 감지에 **`x-requested-with===com.pb.android`** 추가(`isPabaApp`) → instCd/reqPath 없어도 브랜드로 rewrite + instCd=OPBS 보강. OPBS_REDIRECT_MAP 에 index.spc·auth.spc 둘 다 존재. **⚠️ 플랫폼 차이(운영 스카우터 iOS 로그로 확인)**: **iOS 파바앱은 `?instCd=OPBS` 는 보내지만 `x-requested-with` 헤더가 없다**(Android 전용). UA 도 일반 Safari 와 동일 → **iOS 는 instCd 파라미터가 유일 신호**. 감지 우선순위: ①instCd=OPBS(iOS·Android 공통 주신호) ②x-requested-with(Android 보조) ③reqPath. **미해결 리스크**: iOS 파바앱이 param 없이 오면(예: dev index.spc) 구분 수단 전무 → **앱측이 instCd 전송 보장 필요**(운영은 정상 전송 확인). **→ 배포 후 dev 실기기(Android/iOS)로 브랜드 랜딩 + 디버그 alert 신호 확인.**
- [~] (08-05, ⏳배포대기) **[파바앱(OPBS) 페이지 크롬 숨김 — 헤더·브레드크럼·푸터]** 사용자 요청(별도 승인). ⚠️ **오직 파바앱=OPBS 만!** 다른 브랜드(OUNF/NONE) 제외. **회원가입뿐 아니라 모든 OPBS 페이지**(휴면/찾기/정보수정/비번변경/탈퇴/alert 등 — 레거시 `views/brand/member/opbs/*` 전부)에 자동 적용. 규칙: ①상단 글로벌 메뉴(헤더) 숨김+**공백 유지**(헤더 `position:fixed`라 공백은 `.page{padding-top}`이 이미 확보 → 미렌더해도 공백 남음) ②브레드크럼(홈/회원가입) 숨김+**높이 유지**(`visibility:hidden`) ③**하단 푸터 완전 제거**(공백 불필요) ④**챗봇(Happy Bot) 완전 제거** → '해피포인트 생활 첫 단계' 타이틀 세로 위치 일반과 동일 유지. **구현**: (a)`middleware.ts` — `x-hp-inst` 헤더에 instCd 전달(기본=searchParams instCd, OPBS 확정 분기에서 `x-hp-inst=OPBS` 세팅). (b)`layout.tsx`(async) — `isOpbs = x-hp-inst===OPBS && x-hp-path startsWith /page/brand/`(오탐 이중조건) → `<SiteHeader/>`·`<SiteFooter/>` 미렌더 + children 을 `<div data-opbs-chrome>`로 감쌈. (c)`globals.css` — `[data-opbs-chrome] nav[aria-label="현재 위치"]{visibility:hidden}`. **핵심**: 페이지별 개별수정 없이 **instCd=OPBS 신호 기반 레이아웃 단일 지점**에서 전 OPBS 페이지 커버(instCd 는 브랜드 플로우가 채널코드용으로 이미 전파). 일반·타브랜드 무영향. **규칙 일반화(사용자 정정)**: 타이틀 1개가 아니라 **모든 OPBS 페이지의 콘텐츠 시작 위치(상단 여백)가 일반 페이지와 동일**해야 함 = 헤더여백(`.page` padding-top)+브레드크럼여백(visibility:hidden 박스 유지) 둘 다 보존. **커버리지 검증**: 프론트 brand 라우트 23개 중 **UI 렌더 22개 전부 `Subpage`+`Breadcrumb` 사용 → 상단 여백 일반과 동일 자동 충족**. 1개(`member/confirm-complete`)는 **화면 없는 리다이렉트 전용**이라 해당 없음(N/A). → 추가 코드변경 불필요. **→ 배포 후 파바앱 실기기: 헤더/브레드크럼/푸터 숨김 + 타이틀 위치 유지 + OUNF 등 타브랜드는 정상 표시 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 회원가입 instCd URL 파라미터 관통 — 크롬 flash 제거]** 증상: 파바앱 본인인증→약관 넘어갈 때 헤더/푸터가 잠깐 보였다 사라짐(flash). 원인: 회원가입 브리지 3곳(policy/form/optional-form-bridge)이 instCd 를 **쿠키로만** 넘기고 **리다이렉트 Location URL엔 안 붙임** → 다음 페이지 요청 URL에 instCd 없음 → 미들웨어 `x-hp-inst=""` → 레이아웃이 OPBS 미판별 → chrome 렌더(flash). **해결(사용자 지시: 쿠키 병행 + 파라미터로도 관통)**: 브리지 3곳이 이미 바디에서 받는 instCd(policy-bridge=`instCd`, form-bridge=`policyInfo.instCd`, optional-form-bridge=`joinInfo.instCd`)를 **redirect Location에 `?instCd=` 부착**(값 있을 때만, `encodeURIComponent`). welcome 이동은 `ReferralForm`(공유)이 이미 `?id=..&instCd=` 부착 중(무변경). → 전 구간 index→policy→form→optional-form→welcome **URL에 instCd 관통** → 매 홉 미들웨어 x-hp-inst=OPBS → **레이아웃 chrome 처음부터 숨김 = flash 제거**. 쿠키(데이터 캐리어)는 그대로 유지. **범위**: 회원가입 흐름만(휴면/찾기/탈퇴 등 타 OPBS 브리지 흐름은 flash 시 후속). **변경**: `policy-bridge`·`form-bridge`·`optional-form-bridge/route.ts` 3파일. **→ 배포 후 파바앱 실기기로 index→welcome 전 구간 chrome 안 뜸 + 각 URL instCd 유지 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 joinProc 2차 — 약관 쿠키(join_policy_info) 브라우저 복원 배관 누락]** 백엔드 instCd 복호화 수정([ha-web-api W31](../../ha-web-api/worklog/weekly/WORKLOG-2026-W31.md)) 후 dev 재시도 → **복호화 예외는 사라짐(로그에 getPolicyCookie 트랜잭션 진입 확인)** 이나 여전히 code=99. 원인: `joinProc` L189 `getPolicyCookie()==null` → save skip. **약관 쿠키 `join_policy_info`가 브라우저에 없음**. 메커니즘: 백엔드 `/api/brand/join/form`(정보입력 모델API)이 `setPolicyCookie`로 Set-Cookie 하지만 **SSR fetch라 브라우저 미도달** → 프론트가 값 캡처(`form/page.tsx` `ctx.policyCookie`)→`JoinInfoForm` 히든필드 `__join_policy_info`→**브리지가 브라우저 쿠키로 복원**해야 함. **일반 `optional-form-bridge`엔 이 복원 로직 있으나 브랜드 브리지엔 누락**. **수정**: `brand/join/optional-form-bridge/route.ts`에 일반과 동일하게 `__join_policy_info` 추출 + `res.headers.append("set-cookie", "join_policy_info=…; Path=/; HttpOnly; Max-Age=1800; SameSite=Lax")` 추가. → joinProc 클라 fetch에 쿠키 실림 → getPolicyCookie() 정상. **→ 프론트 재배포 후 파바앱 가입 완주 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 크롬 숨김 — 알럿 등 instCd 없는 페이지 누락 보강]** 증상: 브랜드 alert 페이지(`/page/brand/common/alert`)에서 파바앱인데 헤더/푸터 노출. 원인: 레이아웃 `x-hp-inst` 판별이 **URL instCd 에만 의존** → alert 는 여러 곳에서 리다이렉트로 와서 instCd 유실 시 크롬 노출. 수정 `middleware.ts`: `x-hp-inst` 세팅 시 **`x-requested-with===com.pb.android`(파바앱 Android, 모든 요청에 실림)면 무조건 OPBS** → URL instCd 없어도 전 브랜드 페이지(alert·member 흐름 포함) 크롬 숨김. iOS 는 헤더 없어 URL instCd 의존(alert 리다이렉트는 `&instCd=` 포함하므로 대체로 커버). **→ 재배포 후 파바앱 alert·휴면/찾기 등에서 크롬 숨김 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 브랜드 페이지 instCd URL 소비 — 빈 instCd 전파 차단]** 증상: 오류 시 `/page/brand/common/alert?alertType=comm-complete&instCd=`(빈 instCd)로 랜딩 → OPBS 미판별(로컬은 com.pb.android 헤더 없음) → 크롬 노출. 원인: 브랜드 `form`·`optional-form` 페이지가 instCd 를 **쿠키(policyInfo/joinInfo)에서만** 읽음 → 쿠키에 없으면 빈값 → 백엔드 바디·alert 리다이렉트 모두 빈 instCd(→ 백엔드 comm-complete 유발 + 알럿 크롬 노출). 우리가 브리지로 URL 관통시킨 instCd 를 **페이지가 안 읽던 것**. **수정**: `form/page.tsx`·`optional-form/page.tsx` — `instCd = searchParams.instCd(URL 우선) || 쿠키 || ""` + 값 있으면 백엔드 전송 바디(policyInfo/joinInfo)에도 반영. → 오류 알럿까지 instCd=OPBS 관통 → 로컬/iOS 도 크롬 숨김 + 빈 instCd 로 인한 comm-complete 방지. **→ 재배포 후 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 회원가입 완료 페이지 — 레거시 규격에 맞춤]** 기존 브랜드 welcome 이 일반과 다른 문구/구성("회원가입이 완료되었습니다"+"가입 정보" Section)이었음. 레거시 `opbs/join-complete.jsp` 확인 = **일반과 동일 축하 문구**("회원가입을 축하합니다!"+"지금 로그인하고 다양한 혜택을 누려보세요.")+아이디+"해피포인트 시작하기"(goHome 앱복귀). **수정**: `brand/join/welcome/page.tsx` 를 일반 welcome 레이아웃(`join.module.css` completeWrap/completeIcon/completeIdBox 재사용)으로 통일 — 문구·아이디박스 동일, **단 QR/앱다운로드 프로모션 제거**(이미 앱 안), CTA 는 `BrandReturnButton`(앱 복귀) 유지. 데이터(getJoinComplete userId/urlMap)·복귀 로직 그대로. **→ 재배포 후 확인.** (약관/가입폼/옵셔널은 이미 일반과 화면·문구 동일 — 폼 컴포넌트 공유, 대조 확인 완료.)
- [~] (08-06, ⏳배포대기) **[파바앱 완료 "해피포인트 시작하기" 앱복귀 차단 — CSP form-action]** 증상: 완료 페이지 버튼 눌러도 아무 동작 없음(닫기도 안 됨). 콘솔: `Sending form data to 'https://app.pbapp.co.kr/callback/hp/join' violates ... form-action 'self' https://*.happypointcard.com. blocked.` 원인: `BrandReturnButton`이 urlMap 앱복귀 URL(`app.pbapp.co.kr`)로 form POST 시도하나 **CSP `form-action`이 happypointcard.com만 허용** → 차단. (CSP 차단은 JS 예외 아니라 catch의 window.close도 미발동 → 무반응.) **수정**: 처음엔 `form-action`에 `*.pbapp.co.kr` 화이트리스트 추가했으나, **사용자 결정으로 레거시(header-ga4.jsp)와 동일하게 `form-action` 지시자를 미지정(=무제한 개방)** 으로 변경(2026-08-06). 이유: 브랜드 추가마다 CSP 관리 안 하려고. (레거시도 form-action 없음 = 이 벡터 미제한이었음.) `form-action`은 default-src 상속 안 하므로 미지정 시 폼 전송 무제한. **트레이드오프**: XSS 시 폼 유출 방어(2차선) 상실 → 향후 script-src nonce 전환으로 보강 예정. **→ 재빌드/재배포(로컬 dev서버 재시작) 후 앱복귀 POST 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 브레드크럼(홈/회원가입) 숨김 안 먹던 버그 — 셀렉터 교체]** 증상(스샷): OPBS 본인인증 페이지에서 헤더·푸터·챗봇은 숨었는데 **브레드크럼 "홈/회원가입"만 노출**(isOpbs=true인데 브레드크럼 CSS만 미적용). 원인 추정: 셀렉터 `[data-opbs-chrome] nav[aria-label="현재 위치"]`의 **한글 aria-label 매칭이 빌드/인코딩에 따라 불안정**. **수정**: Breadcrumb `<nav>`에 ASCII 마커 **`data-hp-breadcrumb`** 추가(`subpage.tsx`) + globals.css 셀렉터를 **`[data-opbs-chrome] [data-hp-breadcrumb]{visibility:hidden !important}`** 로 교체. → 브레드크럼 확실히 숨김+공백유지. 헤더/브레드크럼은 동일 isOpbs 하나로 제어(분리 불가). **→ 하드리프레시/재빌드 후 확인.**
- [~] (08-06, ⏳배포대기) **[일반 회원가입 exist-member → 비밀번호 재설정 재인증 중복 제거(레거시 정합)]** 사용자 지적: 가입 본인인증 후 기존계정(exist-member)에서 "비밀번호 재설정" 누르면 **본인인증을 또** 시킴. 레거시 대조: 레거시는 `popup.jsp`가 **`find-id-pw-process.spc`로 직접 POST**(findType=pw) → 가입 때 심은 `_AUTH_INFO_TOKEN_` 쿠키 재사용 → **재인증 없이** 비번변경(result-view)으로 감. (레거시 `exist-member.jsp`는 재인증 경로지만, 가입 팝업 경로는 popup.jsp=직행.) 신규 프론트는 `common/alert` exist-member 버튼이 **`find-id-pw-form`(인증화면)으로** 보내 재인증시킨 게 불일치. 신규 백엔드도 exist-member 시점 `JoinModelApiResource:131`에서 `_AUTH_INFO_TOKEN_` 세팅함(재사용 가능). **수정**: 신설 `app/(site)/page/common/reset-pw-button.tsx`(client) — POST `/page/member-info/find-id-pw-process-bridge`{findType:pw, authInfo 미전달→쿠키폴백} → 성공(userId+rpsCd00) 시 `/page/member-info/find-id-pw-process`(비번변경) 이동, 실패(none-auth 등) 시 `find-id-pw-form?findType=pw`(본인인증)로 폴백. `common/alert/page.tsx` exist-member 비번재설정 버튼을 `action:"reset-pw"` 로 이 컴포넌트에 연결. **(08-06 2차 — 폴백만 타던 원인 수정)**: reset-pw POST 가 계속 none-auth → 폴백(find-id-pw-form)만 탐. 원인: exist-member 시점 백엔드가 `_AUTH_INFO_TOKEN_`을 세팅하지만 **`/api/join/policy` 가 SSR 호출이라 Set-Cookie 가 브라우저 미도달** + exist-member 는 form-bridge(브라우저 쿠키 심는 지점) 이전에 종료 → 브라우저에 `_AUTH_INFO_TOKEN_` 없음 → reset-pw 쿠키 재사용 실패. **수정**: `join/policy-bridge/route.ts` 가 `_AUTH_INFO_TOKEN_`(raw authInfo)를 **브라우저 쿠키(path=/, 1800s)로도** 심음 → exist-member alert 에서 reset-pw 가 재사용. 정상 가입은 이후 form-bridge 가 재암호화본으로 덮어써 무해. **→ 배포 후 가입 exist-member → 재인증 없이 비번변경 진입 확인.** (⚠️ 브랜드(파바앱) exist-member 도 동일 패턴 필요 시 후속.)
- [~] (08-06, ⏳배포대기) **[파바앱 아이디/비번찾기 URL 일반 미러링 마이그레이션]** 사용자 지시: 브랜드 find 페이지를 일반과 미러링(`/page/brand/member-info/find-id-pw-{form,process,complete}`)으로. **git mv**: `brand/member/find-auth`→`brand/member-info/find-id-pw-form`, `find-view`→`find-id-pw-process`, `find-complete`→`find-id-pw-complete`(page+error 각각). **갱신**: ①미들웨어 `OPBS_REDIRECT_MAP` `find-id-pw-form.spc`→`/page/brand/member-info/find-id-pw-form` ②각 페이지 canonical ③find-id-pw-form 내부링크→`/find-id-pw-process` ④find-id-pw-process form action→`/find-id-pw-complete` ⑤브랜드 `common/alert` exist-member 비번재설정→`/find-id-pw-process`. 임포트 전부 절대경로(@/)라 무영향. **모델API/interface/lib는 유지**(`/api/brand/member/find-*` 백엔드 그대로 — 기능 동일, front-only 배포). 레거시 대조: find-auth=reqPath"APP" 강제·instCd·findType → 신규 모델API도 `res.put("reqPath","APP")`(BrandMemberModelApiResource:799) 정합. **→ 배포 후 `find-id-pw-form.spc?instCd=OPBS` → 브랜드 find-id-pw-form 랜딩 + form→process→complete 관통 확인.** **(08-06 후속 — interface/lib 파일명도 통일)**: `git mv` interface `page/brand/member/find-{auth,view,complete}.ts` → `page/brand/member-info/find-id-pw-{form,process,complete}.ts`, lib `brand-member-find-{auth,view,complete}-api.ts` → `brand-member-find-id-pw-{form,process,complete}-api.ts`. 페이지·lib 임포트 경로 갱신 + 주석/error 라벨 정리. 함수명(getFindAuth/postFindView/postFindComplete)은 유지. `tsc --noEmit` 소스 에러 0(.next 스테일 캐시만 — 재빌드 시 재생성). **(08-06 추가 — 모델API 엔드포인트도 통일)**: 프론트 lib 3종 엔드포인트를 `/api/brand/member/find-*` → **`/api/brand/member-info/find-id-pw-{form,process,complete}`** 로 변경 + 백엔드 `BrandMemberModelApiResource` @Mapping 3종 동일 변경([ha-web-api W31](../../ha-web-api/worklog/weekly/WORKLOG-2026-W31.md)). ⚠️ **프론트+백엔드 커플링 → 함께 배포 필수**(한쪽만 배포 시 404).
- [~] (08-06, ⏳배포대기) **[파바앱 아이디/비번찾기 — PC차단 + 화면 일반과 통일(FindIdPwFlow 공유)]** 사용자 지시: 파바앱 find 화면이 일반과 다름(일반=탭+KCB인증카드 `FindIdPwFlow`, 브랜드=카드+버튼) → instCd 외 동일하게 + PC차단(파바앱 회원가입처럼). **①PC차단**: 브랜드 `find-id-pw-form` 페이지가 `getFindAuth` 응답 `landing===false` 체크 → `code==="PC"`면 `/page/brand/common/alert?alertType=invalid-access` 리다이렉트(백엔드 `findAuth` 가 PC → `commComplete(code:"PC")` 반환, UA 포워딩으로 SSR도 device 판별). **②화면통일**: `FindIdPwFlow`(일반)를 **파라미터화**(instCd·reqChnl·certReqPath·bridgePath·successPath·joinHref, 기본값=일반이라 일반 무영향) → 브랜드 페이지가 이걸 공유(reqChnl=instCd·certReqPath="APP"·브랜드 경로). **③브랜드 process-bridge 신설**(`find-id-pw-process-bridge/route.ts`): 일반 미러 + instCd, `postFindView` 검증 → `FIND_ID_PW_AUTH_COOKIE`(instCd 포함) set. **④브랜드 process 페이지**: 쿠키 우선 읽기 + searchParams 폴백(alert 직행 대응). ⑤`FindAuthResponse` 타입 landing/landingType 추가. `tsc --noEmit` 소스 에러 0. front-only(모델API 무변경). **→ 배포 후 PC 차단 + 파바앱 find 화면 일반과 동일(탭/인증카드) + 인증→결과 관통 확인.** **(08-06 수정 — 인증 후 '성공' alert만 뜨던 버그)**: FindIdPwFlow `goProcess`가 process 호출에 `reqPath: ""`를 하드코딩 → 브랜드는 `reqPath="APP"`(채널)이 필요한데 빈값이라 `checkMemberByCi`가 정상(00/0000) 미반환 → `landing:false`(userId 없음) → FindIdPwFlow 성공판정 실패 → 래퍼 `message:"성공"`을 alert. 수정: `goProcess` reqPath 를 `certReqPath` 사용(일반 ""·브랜드 "APP"). front-only. **(08-06 진짜 원인 — 백엔드 로그로 확정)**: KCB 인증은 성공(`chnl=OPBS,reqPath=APP,rpsCd=B000`)했으나 `POST /api/brand/member-info/find-id-pw-process` 가 **`parameters={}`**(전부 미바인딩) + `content-type: application/json`. 백엔드 `findView`/`findComplete` 는 **`@RequestParam Map`**(폼 기대)인데 브랜드 lib `postFindView`/`postFindComplete`(legacyContractPost)가 **기본 json 전송** → 미바인딩 → instCd/authInfo 빈값 → 회원조회 실패 → 래퍼 message "성공" alert. (일반 lib `findIdPwProcess` 는 `bodyMode:"form"` 사용 중 — 브랜드만 누락.) **수정**: `brand-member-find-id-pw-process-api.ts`·`brand-member-find-id-pw-complete-api.ts` 에 **`bodyMode:"form"` 추가**. + FindIdPwFlow fall-through alert 를 래퍼 "성공" 대신 실제 result 코드 표시로 개선(진단 겸용). front-only.
- [~] (08-06, ⏳배포대기) **[파바앱 전 완료화면 앱복귀 제거 → 창 닫기 통일]** 사용자 지시: 파바앱(OPBS) 회원가입 완료·아이디비번찾기 등 **모든 완료/결과/안내 화면에서 앱복귀(urlMap→앱콜백 POST) 전부 제거, `window.close()`로 통일**(웹홈/로그인/앱다운로드 이동도 없음). **수정**: ①`BrandReturnButton` → urlMap POST 로직 삭제, 무조건 `window.close()`(prop `label`만). ②`join/welcome`·`common/alert`(fetchUrlMap 제거)·`member-info/find-id-pw-complete`(로그인링크 제거)·`member/{dormancy,modify-info,modify-pw,withdrawal}-complete`(urlMap "앱으로 돌아가기"/"확인" 링크 제거) → 전부 `<BrandReturnButton label="확인"/>`. unused import 정리. `tsc` 소스 에러 0. front-only(백엔드 urlMap 반환은 두되 프론트가 미사용). **→ 배포 후 각 완료화면 [확인]→창 닫힘 확인.**
- [~] (08-06, ⏳배포대기) **[파바앱 아이디/비번찾기 — 미가입 시 브랜드 alert 노티]** 증상: 파바앱 find 본인인증 성공했으나 **가입된 아이디 없음(미가입)** 시 JS alert("회원 정보를 확인하지 못했습니다") 노출. 요청: 브랜드 alert 페이지에 타입 추가해 노티 + [확인]→close. **수정**: ①`FindIdPwFlow` 에 `notFoundHref` prop 추가 — goProcess 폴백(none-auth/휴면 외 미가입 등)에서 값 있으면 JS alert 대신 그 URL 로 랜딩(브랜드만). ②브랜드 find 페이지가 `notFoundHref=/page/brand/common/alert?alertType=no-member&instCd=` 전달. ③브랜드 `common/alert` 에 **`no-member`** 타입 추가("가입된 아이디가 없습니다" + `kind:return`→BrandReturnButton→window.close). front-only. `tsc` 소스 에러 0. **→ 배포 후 파바앱 find 미가입 시 브랜드 안내페이지 + [확인]→창닫힘 확인.**
- [~] (08-06, ⏳배포대기) **[일반 휴면계정 해제 흐름 신규 구축]** 기존 프론트는 `/page/dormancy/auth-form` **placeholder(레거시 링크만)**, 입력·완료 화면 없음. 백엔드 모델API(`/api/dormancy/{auth-form,auth-process,dormancy-process,status}`)는 존재. **레거시 URL 유지**(`/page/dormancy/*`) + 모델API 정합으로 신규 구축. **흐름**: 회원가입 본인인증→휴면감지→`common/alert(dormancy)`→[휴면해제]→`/page/dormancy/auth-form`(KCB **재인증**)→auth-process(휴면회원이면 프리필)→`/page/dormancy/dormancy-view`(정보확인+[휴면해제])→dormancy-process→`/page/dormancy/dormancy-complete`. **신규 파일(8)**: `lib/dormancy-api.ts`(authProcess/releaseProcess, **bodyMode:"form"** — @RequestParam), `lib/dormancy-cookies.ts`, `auth-form/{page.tsx(재작성)+dormancy-auth-flow.tsx(cert)}`, `auth-process-bridge/route.ts`(프리필쿠키), `dormancy-view/{page.tsx+dormancy-release-button.tsx}`, `dormancy-process-bridge/route.ts`(결과쿠키), `dormancy-complete/page.tsx`. 계약 타입 `interface/page/dormancy/auth-form.ts` 활용. `tsc` 소스 에러 0. **⚠️ 백엔드 1정합 커플링**(아래) → 함께 배포. **encMnm 검증**은 `sha512(이름|전화)`만(reqChnl/reqPath는 로깅용)이라 release 단순. **→ 배포 후 휴면 전 구간 확인.**
- [~] (08-06, ⏳배포대기·키대기) **[일반 회원가입 완료 Airbridge createDeeplink — CDN 방식(젠킨스/lockfile 무관)]** 이전 airbridge 시도는 npm dep(`airbridge-web-sdk-loader`) 추가로 frozen-lockfile 배포 실패했음(08-05 롤백). 이번엔 **CDN 스크립트 로드**로 재구현 → **package.json/lockfile 무변경, 서버 설정 무관**. **신설** `join/welcome/airbridge-signup-deeplink.tsx`(client): `static.airbridge.io/sdk/latest/airbridge.min.js` 동적 로드 → `init({app,webToken})` → `createDeeplink({channel:"ha-web",campaign:"ha-web-sign-up",fallbacks:{ios/android 스토어}})` → 버튼 href. **키(env) 미설정/SDK 실패 시 스토어 폴백**(그레이스풀). 일반 `join/welcome` 만 교체(`AppOpenCta`→이것), **파바앱 `brand/join/welcome` 미적용**. **env**: `.env.{example,dev,stg,prod,local}` 에 `NEXT_PUBLIC_AIRBRIDGE_{APP,WEB_TOKEN}` 빈 플레이스홀더(사용자가 개발/운영키 입력). **CSP**(next.config): script-src `static.airbridge.io` + connect-src `*.airbridge.io *.abr.ge` 추가. `tsc` 0. front-only. **→ 사용자 키 입력 후 배포·실기기 딥링크 확인**(createDeeplink option 은 키 넣고 실동작 보며 조정 여지). **(08-06) 키 입력 완료**(.env 4종 APP·WEB_TOKEN 채움). **🧹 테스트 로그(제거예정)**: `airbridge-signup-deeplink.tsx` 에 `console.log("[airbridge][TEST] createDeeplink 결과:", url)` 등 4곳(키미설정/SDK실패/생성결과/예외) 추가 — 딥링크 URL 콘솔 노출용. **딥링크 확인 끝나면 제거.** **(08-06) 원래 버튼 링크(=앱 스토어)를 딥링크 목적지에도 명시**: 기존 `AppOpenCta` 버튼 링크가 스토어(`storeLinks.google=play.google.com/store/apps/details?id=com.hpapp`·`apple=apps.apple.com/.../id376313183`)였음 → `createDeeplink` 에 `deeplinks:{ios:apple,android:google,desktop:google}` 추가(기존 `fallbacks` 유지). ⚠️ `deeplinks` 는 원래 "앱 설치 시 여는 앱 스킴/유니버설 링크" 자리인데 **확정 앱 스킴이 아직 없어** 원래 버튼 동작(스토어 이동) 그대로 스토어 URL 사용 — 앱팀이 스킴 주면 `deeplinks`만 교체(주석 명시). **(08-06 사용자 지정 URL 확정)**: 딥링크 목적지 = **이벤트 페이지** `https://napi.happypointcard.com/page/event/event-view.spc?eventseq=fbdf2fc2552dae5e87e37548110a1d59`(상수 `DEEPLINK_URL`), 에어브릿지 오류(키 미설정/SDK 실패/생성 실패) 및 미설치 폴백 = **메인** `https://napi.happypointcard.com/page/main/index.spc`(상수 `MAIN_FALLBACK`). `storeLinks` import 제거.
- **(08-06 dev 배포 → SDK 초기화 오류 → 공식 규격으로 전면 재작성)**: 증상: 콘솔 `airbridge.min.js: Uncaught Error: Failed to load Airbridge: SDK instance not found. Ensure createAirbridge() was called before loading the SDK.` → 내 코드가 min.js 를 직접 로드 후 `createDeeplink` 호출했으나 **min.js 는 단독 실행 불가**(공식 부트스트랩 스니펫이 `window.airbridge` 큐 스텁 생성 + min.js 를 **자체 로드**해야 함). **잘못 쓴 것 3가지**: ①함수명 `createDeeplink`(❌) → 실제 **`createTrackingLink(channel, options, onSuccess, onError)` 콜백 기반**. ②옵션 `deeplinks/fallbacks`(❌) → 플랫 **`deeplink_url`·`fallback_ios/android/desktop`·`campaign`**. ③min.js 직접 로드(❌) → 부트스트랩 스니펫이 자체 로드. **수정**(공식 문서 help.airbridge.io 규격): 컴포넌트에 공식 부트스트랩 스니펫(`BOOTSTRAP_SNIPPET`)을 인라인 `<script>`로 1회 주입(`ensureAirbridge`) → `airbridge.init({app,webToken})` → `createTrackingLink("ha-web", {campaign:"ha-web-sign-up", deeplink_url:이벤트URL, fallback_*:메인}, onSuccess(link→shortURL), onError(→메인폴백))`. 성공 시 `link.shortURL` 을 버튼 href. 인라인 스니펫이라 여전히 **npm dep/lockfile 무관**(CSP script-src 'unsafe-inline' 허용). 🧹 테스트 로그(키미설정/스니펫실패/생성성공/생성실패) 유지. **→ 재배포 후 콘솔 `createTrackingLink 성공:` + shortURL 확인.**
- **(08-06 SDK 설치 범위 결정 → 전역(<head>, 모든 페이지, 파바앱 포함) 이관)**: 사용자 결정 — 에어브릿지를 트래킹 링크 생성뿐 아니라 **웹 방문/유입/전환 어트리뷰션 전체**로 쓰기 위해 공식 권장대로 **전역 설치**(파바앱 포함). **신설** `components/analytics/airbridge-root-script.tsx`(서버, `next/script` **beforeInteractive** 로 부트스트랩 스니펫+`airbridge.init({app,webToken})` 주입 — GA4 `Ga4RootScripts` 패턴 미러, 키 미설정 시 null). 루트 `app/layout.tsx` `<head>` 에 `<AirbridgeRootScript/>` 추가(Ga4 다음). → 모든 페이지에서 `window.airbridge` 큐 스텁+min.js 자체 로드. **welcome 컴포넌트 단순화**: 스니펫 주입(`BOOTSTRAP_SNIPPET`/`ensureAirbridge`) 제거 → 전역 로드된 `window.airbridge.createTrackingLink` 만 호출(옵션·폴백 동일). 인라인 스니펫이라 npm dep/lockfile 무관 유지. CSP 기존 그대로(script-src `static.airbridge.io`+`'unsafe-inline'`, connect-src `*.airbridge.io *.abr.ge`). **→ 재배포 후: ①전 페이지 `<head>` 에 airbridge min.js 로드 ②welcome 콘솔 `createTrackingLink 성공:`+shortURL ③파바앱 페이지도 SDK 로드(어트리뷰션) 확인.** **✅ (08-06 dev 배포 확인) `createTrackingLink 성공: {shortURL:"https://abr.ge/nq79x84", qrcodeURL:...}` — 트래킹 링크 정상 생성.**
- **(08-06 딥링크/폴백 URL 환경별 env 이관)**: 하드코딩 URL → **env 변수**로 분리(사용자 지정 환경별 napi). 추가 키 `NEXT_PUBLIC_AIRBRIDGE_DEEPLINK_URL`·`NEXT_PUBLIC_AIRBRIDGE_FALLBACK_URL` — `.env.{local,dev}`=`https://dev-napi.happypointcard.com/page/main/index.spc`, `.env.stg`=`https://stg-napi...`, `.env.prod`=`https://napi...`(딥링크·폴백 동일값). `.env.example` 빈 플레이스홀더+주석. 컴포넌트 `DEEPLINK_URL`/`MAIN_FALLBACK` 을 `process.env.NEXT_PUBLIC_AIRBRIDGE_{DEEPLINK,FALLBACK}_URL || 운영napi`(미설정 시 운영 폴백)로 변경. ⚠️ NEXT_PUBLIC_* 는 빌드타임 인라인 → 환경별 빌드 필요. (참고: 이전 딥링크=이벤트페이지였으나 사용자 지시로 **딥링크·폴백 모두 main/index.spc(환경별)** 로 통일.)
- **(08-06 UX 재설계) 진입 시 링크 킵 + 모바일 스크립트 이동 + PC 에어브릿지 QR**: 사용자 지시로 `AirbridgeSignupDeeplink` 를 프로모션 컴포넌트로 재작성. ①**페이지 진입 즉시** `createTrackingLink` 1회 호출 → `shortURL`·`qrcodeURL` 상태 킵. ②**모바일**: "해피포인트 앱으로 이동하기" 클릭 시 **href 이동이 아니라 스크립트**(`window.location.assign(shortURL)`). ③**PC**: 기존 정적 QR(`/happy-point-app-qr.svg`) 자리에 **에어브릿지 `qrcodeURL` 이미지** 노출(생성 전/실패 시 정적 QR 폴백). 컴포넌트가 QR+캡션+버튼(promo 내부)을 함께 렌더(prop `qrClassName`/`captionClassName`/`buttonClassName`) → PC=QR·모바일=버튼은 기존 CSS(`@media max-width:480px` completeQrBox/completeAppCta 토글) 그대로 활용. `welcome/page.tsx` 는 img+캡션+버튼 3개 → `<AirbridgeSignupDeeplink .../>` 1개로 치환. 캠페인 `ha-web-banner-sign-up`. createTrackingLink `fallback_*`=napi(DEEPLINK_URL), 에러 폴백=ERROR_FALLBACK(env). CSP img-src `https:` 라 abr.ge QR 이미지 허용. **→ 재배포 후: PC 에서 에어브릿지 QR 노출·스캔, 모바일 버튼 클릭 시 shortURL 이동 확인.**
- **(08-06) PC QR — 에어브릿지 qrcodeURL 이미지가 브라우저에서 깨짐 → 로컬 생성 data URL 로 대체**: 증상: PC 완료 페이지 QR 이 깨진 이미지(alt 만). 진단: `qrcodeURL`(예 `https://abr.ge/1iouv1.qr`)은 curl 로 보면 **정상 QR PNG(200, image/png, 250×250)** 이고 우리 CSP `img-src 'self' data: blob: https:` 도 abr.ge 허용인데도 브라우저에서만 안 뜸(외부 이미지 로딩 이슈). **해결(사용자 승인)**: 외부 이미지/CDN 런타임 의존 제거 — **shortURL 을 로컬 QR 인코더로 직접 생성 → data URL(`<img src="data:...">`)**. `data:` 는 어떤 CSP 에서도 허용 → 도메인/네트워크/CSP 이슈 완전 회피. **벤더링**: `qrcode-generator@1.4.4`(Kazuhiko Arase, MIT)를 `app/(site)/page/join/welcome/vendor/qrcode-generator.js`(+`.d.ts`)로 **로컬 포함**(npm dep/lockfile 무관). 컴포넌트 `toQrDataUrl(shortURL)=qrcode(0,"M").addData().make().createDataURL(6,12)` → `qrDataUrl` state → PC img src. 생성 전/실패 시 정적 QR(`/happy-point-app-qr.svg`) 폴백. QR 이 인코딩하는 값=동일 shortURL(스캔 동작 동일). 모바일 버튼(shortURL 스크립트 이동)은 그대로. **→ 재배포 후 PC QR 정상 렌더+스캔 확인. (⏳ 왜 abr.ge 이미지가 브라우저에서 깨지는지 근본원인은 미규명 — Network/Console 미확인, 로컬생성으로 우회.)**
- **(08-06) 기본(생성 전) 링크를 abr.ge 사전생성 링크로 + 진입 즉시 기본 QR 렌더**: 사용자 지시 — 에어브릿지 `createTrackingLink` 가 4~5초 걸리므로 **생성 전(그리고 실패 시) 기본 링크**를 abr.ge 사전생성 링크로 노출하고, 성공 시 shortURL 로 대체. **env `NEXT_PUBLIC_AIRBRIDGE_FALLBACK_URL` = abr.ge 플레인 링크**(local/dev=`https://abr.ge/si9punw`, stg/prod=`https://abr.ge/3sndui5`) — 이미 세팅됨. 컴포넌트: 상수 `ERROR_FALLBACK`→`DEFAULT_LINK`. **초기 state 를 기본 링크로**: `shortURL=DEFAULT_LINK`, `qrDataUrl=toQrDataUrl(DEFAULT_LINK)`(lazy init → SSR/최초렌더부터 기본 QR 표시, flash 없음). 생성 성공 시 `shortURL`·`qrDataUrl` 둘 다 shortURL 기준으로 교체. 실패/SDK미로드 시 기본 유지. `toQrDataUrl` 내부 try/catch(실패 시 "" → 정적 QR 폴백). **PC QR 은 `.qr` 외부 이미지 대신** 동일 링크(`abr.ge/si9punw`)를 로컬 인코딩(스캔 동작 동일, 렌더 안 깨짐). 모바일 버튼: 기본엔 abr.ge 링크로 JS 이동, 생성 후 shortURL 로 JS 이동. **→ 재배포 후: 진입 즉시 기본 QR/버튼링크 → 4~5초 후 생성 링크로 교체되는지 확인.**
- **(08-06) 🧹 문의용 진단 로그 보강(제거예정)** — Airbridge 담당자 문의용("링크 클릭해 앱 구동해도 대시보드 카운팅 안 됨, 왜?"). 콘솔에 **소스코드 그대로** 노출: ①`airbridge-root-script.tsx` 전역 init(`airbridge.init({app,webToken})` + app/webToken/sdk src) ②`airbridge-signup-deeplink.tsx` `[airbridge][문의용] 설정 덤프` 그룹(APP_ENV·SDK src·init 스크립트·app·webToken·createTrackingLink 호출문·channel·options) + 생성 결과(shortURL/qrcodeURL) + **소요 ms**(`performance.now()`). 마커 `[airbridge][문의용]` 로 grep 제거 가능. **⚠️ 웹SDK 토큰 콘솔 노출(문의 목적, 이미 클라 번들 공개값). 문의 끝나면 기존 `[airbridge][TEST]` 로그와 함께 전량 제거.** **(08-06 운영 제외 가드 추가)**: `NEXT_PUBLIC_APP_ENV !== "prod"` 일 때만 출력 — **local/dev/stg 만, 운영(prod) 미출력**(prod 빌드엔 diag 문자열 자체가 `""` 라 토큰도 콘솔에 안 찍힘). 클라(`DIAG`)·전역스크립트(`diag`) 양쪽 가드.
- **(08-06) 캠페인 값 보드 등록값으로 통일**: `AIRBRIDGE_CAMPAIGN` `ha-web-banner-sign-up` → **`ha-web-sign-up`**(채널 `ha-web` 동일). 이유: 사용자가 백오피스에 채널 `ha-web`/캠페인 `ha-web-sign-up` 트래킹 링크를 수동 등록 → SDK `createTrackingLink` 도 동일 채널/캠페인으로 맞춰 **같은 캠페인으로 클릭/전환 합산 집계**. (에어브릿지 집계는 링크 객체가 아니라 channel/campaign(+파라미터) 차원 기준 — SDK 동적 링크는 보드 목록엔 미표시, 리포트/실시간에서 채널·캠페인으로 집계. 실시간 이벤트는 "생성"이 아니라 "클릭" 시 발생.)
- **(08-06 ★빌드 env 미선택 버그 확정·수정) NEXT_PUBLIC_* 가 전 환경 `.env.local`(dev) 로 구워지던 문제**: 증상 — 스테이징 빌드로 만든 에어브릿지 트래킹 링크(`abr.ge/qy9082`)를 클릭하면 딥링크가 **dev-napi** 로 감(스테이징인데). 원인 — `package.json` `"build":"next build"` 가 **빌드타임에 env 파일 미지정**. `NEXT_PUBLIC_*` 는 빌드타임 인라인인데, `next start` 스크립트만 `dotenv -e .env.{dev,stg,prod}` 이고 **`next build` 는 Next 기본 규칙으로 `.env.local` 최우선 로드** → `.env.local`(커밋됨, dev-napi/dev GA4) 값이 **모든 환경 빌드에 구워짐**(airbridge URL·GA4·Amplitude 전부 잠재 영향). 이전부터 "빌드가 .env.local 로드" 로 적어둔 미해결 이슈의 실체. **수정**: `package.json` 에 **`build:dev`/`build:stg`/`build:prod`**(`dotenv -e .env.<env> -- next build`) 추가. dotenv-cli 가 값을 `process.env` 에 먼저 주입 → **Next 는 기존 process.env 를 .env.local 로 덮어쓰지 않음** → .env.local 존재해도 환경값 정확 인라인. **⚠️ 배포 절차 조치(사용자)**: 이 프로젝트는 **pm2**(`ecosystem.config.js`, `start:$HP_ENV` 런타임)로 기동하고 재배포는 수동 **`pnpm install && pnpm build && pm2 reload`**(주석). 여기서 `pnpm build`(plain)가 `.env.local`(dev) 을 구워 넣던 게 원인 → 재배포 절차를 **`pnpm build:$HP_ENV`**(예 stg: `pnpm build:stg`)로 변경해야 함. `ecosystem.config.js` 주석도 `pnpm build:$HP_ENV` + NEXT_PUBLIC_* 빌드타임 경고로 갱신함(재배포 SOP 정본). **→ 스테이징 서버에서 `HP_ENV=stg; pnpm build:stg && pm2 reload` 로 재빌드 후 링크 재생성 → 딥링크가 stg-napi 로 가는지 확인. (운영은 build:prod → napi.)**
- [x] (08-06, ✅배포확인) **[공지 상세 빌드 실패 — `sanitizeHtml is not defined` 수정]** ✅ 재배포 후 사용자 확인 — 공지 상세 오류 사라지고 정상 렌더(빌드 통과·페이지 정상). 젠킨스 dev-j 클린 빌드가 `Failed to collect page data for /page/customer/notice-view` → `ReferenceError: sanitizeHtml is not defined` 로 실패(빌드 전체 exit 1). 원인: `lib/legacy-sanitize.ts` 가 `sanitize-html` 을 **함수 내부 지연 require** 로 바꾸며 import 를 `import type` 로 변경했는데, **모듈 최상위 `options` 객체가 여전히 런타임 값 `sanitizeHtml.defaults.allowedTags` 를 참조** → 런타임 바인딩 없어 모듈 평가 단계에서 ReferenceError(클린 빌드=Turbopack production 에서 표면화). **수정**: `options` 를 최상위 상수 → **`buildOptions(sh)` 지연 생성 함수**로 변경, `sanitizeLegacyHtml` 이 sanitize-html 로드 후 `buildOptions(sanitizeFn)` 로 1회 생성해 캐시(`let options`). 최상위에서 sanitizeHtml 런타임 참조 제거. voteView 도 같은 `sanitizeLegacyHtml` 사용이라 함께 해소. ⚠️ **airbridge 변경과 무관한 기존 버그**(dev-j `f3745ce` 부터 존재, 클린 .next 빌드에서만 터짐). **main 에도 동일 버그 존재**(병합이 이 파일 그대로 가져옴) → main 배포 전 동일 수정 필요. **→ 재배포로 빌드 통과 확인.**
- [~] (08-06, ⏳배포대기) **[게시판 상세 오류 시 브랜드 정적 에러페이지(error.html=50x.html) 노출]** 사용자 지시: 게시판 상세(공지 등) 오류 시 사용자가 준 `error.html`("잠시 후 다시 확인해 주세요!" SPC 브랜드 화면)을 보여달라. 확인 결과 **동일 디자인이 이미 레포에 2곳 존재** — `public/50x.html`(정적, error.html과 바이트 동일)·`app/global-error.tsx`(React 최후 방어선). 문제: ①route `error.tsx` 는 (site) 레이아웃(헤더/푸터) 안에서 렌더 → 독립 전체화면 error.html 재현 불가 ②하드500은 error.tsx/global-error 도 못 잡음(→인프라 프록시 5xx→/50x.html 필요, 별건). **처리**: `notice-view/error.tsx`·`voteView/error.tsx`(게시판 상세 2종, 같은 sanitizeLegacyHtml 사용) 를 **`window.location.replace("/50x.html")`** 로 변경 → 렌더 오류 시 독립 전체화면 브랜드 정적 페이지(=error.html) 그대로 노출. `/50x.html` 은 미들웨어 matcher(`.*\.` 확장자 제외)로 정적 서빙 확인. 위 sanitize 수정으로 하드500이 렌더오류로 내려오므로 이 error.tsx 가 실제로 발동. **→ 배포 후 게시판 상세 오류 시 50x.html 노출 확인.** (다른 라우트 error.tsx 는 기존 개별 메시지 유지 — 사용자 범위 지정.) **🧹 테스트**: `notice-view/page.tsx` `?forceError=1` 강제 throw 블록 → **✅ 제거 완료(2026-08-06, 검증 후).**
- **(08-06) 에러 화면 깨짐 원인 + `/page/error` 라우트로 전환**: 사용자 확인 — `/50x.html` 화면이 레거시와 너무 다름(깨져 보임). **원인**: 우리 CSP `style-src 'self' 'unsafe-inline'` 가 외부 `common.css`(front.happypointcard.com) **차단** → 스타일 미적용(이미지는 img-src https 로 떴지만 CSS 없어 raw). **수정**: ①next.config CSP `style-src` 에 **`https://front.happypointcard.com` 추가** → 레거시 common.css 로드 → 화면 동일. ②**`/page/error` 라우트 신설**(`app/(site)/page/error/page.tsx`) — 레거시 error.html 동일 마크업(#wrap…)+common.css, `robots noindex`. ③`(site)/page/layout.tsx` — `x-hp-path` 가 `/page/error` 면 **헤더/푸터/챗봇 숨김**(OPBS 와 동일 hideChrome) → 전체화면 standalone. ④게시판 상세 error.tsx(notice-view·voteView) 리다이렉트 `/50x.html` → **`/page/error`**(URL 이 /page/error 로 찍힘). public/50x.html·global-error.tsx 는 인프라/루트크래시용으로 유지. **→ 배포 후 `?forceError=1` 로 /page/error 화면이 레거시와 동일한지 확인.** **✅ (2026-08-06 사용자 확인) `?forceError=1` → /page/error 정상, 레거시와 동일 화면(common.css 적용). 테스트코드 제거 완료.** — `sanitize-html`은 dev에 설치됨(`require.resolve` OK)이나 Next **Turbopack 프로덕션 빌드 + `serverExternalPackages`** 의 external require 가 `Cannot find module 'sanitize-html'` 로 실패. 로컬 프로덕션 빌드(=webpack? 확인 필요)는 200. **클린 재빌드(`rm -rf .next && pnpm build`) 결과 확인 → 여전히 500이면 Turbopack external resolve 이슈 확정 → 대응: 웹팩 빌드 전환 or 번들/hoist 방식**. 별개로 dev 빌드가 `.env.local` 로드(→NEXT_PUBLIC_* local로 구워짐) — 빌드도 `.env.dev` 로드하도록 교정 검토. (내일 진행)
- [ ] ⚠️ (08-06, **미완료·사용자 직접 수정 예정**) **[파바앱(OPBS) rewrite URL 정책 미완료]** — 사용자가 rewrite URL 을 더 손봐 수정할 예정 → **파바앱 rewrite 정책은 아직 완료 아님.** 근거: 레거시 정본 `ha-web RedirectUtil.sendPbAppRedirect` 분석 결과 **회원가입 본인인증 유입 = `/page/join/auth.spc?instCd=OPBS`(+as,ad)** 이고 **`/page/join/index.spc` 처리는 레거시에 없음**. 현재 `middleware.ts` `OPBS_REDIRECT_MAP` 의 `/page/join/index.spc` 매핑은 2026-08-05 dev 디버그 관찰로 넣은 것이나 레거시·운영 스카우터(auth.spc + `x-requested-with=com.pb.android`)와 불일치 → **재검토/정리 대상**(index.spc 라인 제거 여부 등은 사용자 수정 시 확정). 주석은 현행화 완료(middleware.ts:24). ⚠️ 이 항목 열려있는 동안 파바앱 rewrite 관련은 `⏳미완료` 로 둔다.
- [ ] 🧹 (08-06, **장기·문의완료 후 삭제**) **[Airbridge 문의용 진단 로그 제거]** — Airbridge 담당자 문의 끝나면 아래 진단/테스트 로그 **전량 제거**: ①`airbridge-signup-deeplink.tsx` `[airbridge][문의용]` 그룹 덤프 + `DIAG`/`AIRBRIDGE_APP`/`AIRBRIDGE_WEB_TOKEN`/`AIRBRIDGE_SDK_SRC` 상수 + `performance.now()` ms 측정 + 기존 `[airbridge][TEST]` 잔재 ②`airbridge-root-script.tsx` `initDisplay`/`diag` + `[airbridge][문의용]` init 콘솔. **grep 마커: `문의용` · `[airbridge][TEST]`**. 현재 `NEXT_PUBLIC_APP_ENV !== "prod"` 가드로 운영 미출력(=운영엔 안 나가지만 코드는 남아있음 → 문의 종료 후 코드 자체 제거). ⚠️ 웹SDK 토큰 콘솔노출 코드라 문의 종료 즉시 정리.
- [ ] (08-04) **[가입완료/Airbridge] 회원가입 완료(welcome) 시 Airbridge UTM에 유저 식별자 추가** — 로그인(가입 완료) 상태이면 **userId를 SHA-512로 해시**한 값을 Airbridge 이벤트/UTM에 함께 전송(개인정보 원문 대신 해시). 위치: `app/(site)/page/join/welcome/`(welcome 화면). 가입 완료 시점에만 발생. (userId는 welcome이 `_HPC_USER_ID_` 쿠키/쿼리로 이미 보유)
- [ ] (07-31) modify-info-form: 프론트가 `result.alertType`(need-confirm-pw/need-ownership) 분기 처리(현재 result.code만 봐서 깨짐) + 백엔드 alertType 숫자 prefix(`1need-…`) 제거
- [ ] (08-07, 감사발견) **[회원가입 14세미만 보호자 인증/동의 재점검]** 08-07 4흐름 감사 파생. 회원가입 14세미만(`isUnder14`) 보호자 동의·본인인증(`useKcbCert reqPage='parent'` → `P_authYn/P_authInfo/ptorFamyRelCd=90`)이 §19에서 구현됐으나 **실기기 E2E 미검증**(휴대폰/아이핀/14세 경로). 추가로 파바앱(OPBS) 회원가입 14세미만 경로도 동일 동작하는지 미확인. → 14세미만 가입 전 구간(일반+파바앱) 보호자 필드 전송·차단·백엔드 검증 실검증 필요.
- [ ] (08-07, 감사발견) **[일반 휴면 복원 — onlnId 공백(오프라인전용) 회원 아이디 입력 UI 부재]** 휴면 검증 에이전트 [심각#1]. 레거시 `dormancy-view.jsp:196-217`은 `memberDetail.onlnId` 가 비면 **아이디 입력+중복확인 필드 노출** + 제출 시 `checkIdResult!='Y'` 차단. web2 `dormancy-view/dormancy-release-form.tsx` 에는 해당 UI 없음 → 온라인 ID 없는 휴면회원 진입 시 `userId=""` 전송 → 백엔드 `MB2280H0.setOnlnId("")` 필수값 실패/빈ID 복원 우려. → onlnId 공백 분기에 아이디 입력·중복확인 UI 추가 또는 진입 차단. (참고: 파바앱 휴면도 동일 패턴 점검) [[page-model-api-rule]]
- [x] (07-31→08-03 스테이징 셋팅 완료) ★ **[인프라] ALB 앱쿠키 stickiness 설정** — 백엔드 TG(`stg-app-web-api-tg`) 애플리케이션 기반 stickiness, 앱 쿠키명 **`HA_AWSALB`**, duration 1h. 프론트 TG는 ELB 세션 스티키(`AWSALB`, 쿠키명 분리로 충돌 없음). **남은 검증**: 백엔드 healthy 타겟 2대 등록.
- [x] (08-03 구현 완료, dev 검증 대기) ★ **[프론트] 모든 SSR 백엔드 호출에 요청 쿠키 forward 통일** — `legacyContractGet/Post` 기본 `forwardCookies:true` 전환(→`pingModel` 자동 커버), `join/policy` raw fetch에 `cookie: store.toString()` 추가. 요청 쿠키 전체 전달로 `AWSALBAPP`+`HA_AWSALB`+`JSESSIONID` 커버. (§20)
- [x] (08-03 구현 완료, dev 검증 대기) [백엔드] `HA_AWSALB` 발급 — `StickinessCookieFilter` 신설 + `web.xml` `/*` 매핑. **검증**: 브라우저 Set-Cookie 전파 + 세션 TTL·스케일인 재분배 엣지 점검(스테이징 배포 후).
- [취소/안함] (08-05 결정) ~~(대안 보류) Spring Session + Redis(ElastiCache)~~ — 앱쿠키(`_HPC_AUT`) 무상태 방식으로 안착·라운드로빈 검증 완료 → **채택 안 함**(진행 종료).
- [ ] (07-31) 그룹2 신규 스텁 API 실로직 이식(레거시 컨트롤러 기반) 여부 결정
- [ ] (07-31) 그룹3(정적: about/points/services/mypage-inquiry 등) 처리 방침 결정
- [ ] (07-31) 브랜드 policy 이후(join-view/optional/complete)로 instCd·authInfo 전파 검증(쿠키/API)
- [ ] (07-31) cert `request.spc`가 reqChnl=instCd(브랜드) 처리에 문제없는지 dev 확인
- [x] (08-02 완료) 회원가입 none-auth 근본원인 해결 — Edge 바디읽기→Route Handler, 브리지쿠키 one-time 삭제블록 제거(§17).
- [ ] (07-29) 로그인 nonce(+CAPTCHA/rate limit) 실제 구현 여부 결정
- [ ] (07-29) `AuthProvider` 최종 노출 필드 범위 확정(로그인여부 최소화 vs GA용 mbrNo)
- [x] (완료) 약관동의 → 정보입력폼 authInfo 연동(HttpOnly `_AUTH_INFO_TOKEN_` relay, §16).
- [x] (08-03 확인) ELB `/api` 라우팅 = strip 아님, `/api/*` 전부 백엔드로 감(§21). cert 매핑 `/api/page/cert` 정상.
- [ ] 신규 계약 API에 brand join-view(복호화 정보 반환) 존재 여부 확인(일반 join은 완료)
- [ ] `/api/cert/log` 디버깅 로깅 제거(운영 전)
- [ ] 로그인 세션 유지 후속(마이페이지 등 게이트)

## 참고
- 상세 이전 주차: [WORKLOG-2026-W30](./WORKLOG-2026-W30.md)
- 백엔드 짝: [ha-web-api INDEX](../../../ha-web-api/INDEX.md)

## 14. (2026-07-31) Login empty model API
- `app/(site)/page/auth/login/page.tsx` now calls `await pingModel("/api/auth/login")` at page entry.
- The login page query (for example `returnUrl`) is therefore logged as `param={...}` and the empty backend response as `model={...}` in local/dev/stg only, following the existing `pingModel` logging rule.

## 15. (2026-07-31) One-time POST bridge cookie
- Standardized the POST-to-GET RSC bridge cookie naming rule as `hpw_` plus the target URL segments after `/page/`: `/page/join/policy` becomes `hpw_join_policy`.
- `getPostBridgeCookieName()` is shared by middleware and RSC pages. Current flows are `hpw_join_policy`, `hpw_join_form`, and `hpw_brand_member_policy`; each keeps its own cookie `Path`, so values cannot collide.
- On the target GET request, middleware passes the original request cookie to the RSC once and returns `Set-Cookie` with `maxAge: 0` for the matching path. The browser therefore removes the bridge cookie immediately after it is consumed.

## 16. (2026-08-02) 회원가입 policy→form `_AUTH_INFO_TOKEN_` 쿠키 자동연동 (SSR 유지)
- **문제**: 백엔드 `join/policy`가 재암호화 토큰(`flagCertifiTime=Y`)을 `_AUTH_INFO_TOKEN_` 쿠키(Set-Cookie)로 심는데, `policy/page.tsx`가 **SSR fetch**로 호출 → 이 Set-Cookie가 Next 서버까지만 오고 **브라우저로 전파 안 됨**. 다음 단계 `form()`은 이 쿠키로 인증 판별 → 항상 `none-auth`.
- **원인(구조적)**: Next의 서버 `fetch`엔 쿠키 jar 없음 + **RSC(page 렌더)는 `cookies().set()` 불가**(Route Handler/Server Action/middleware 에서만 가능). 모놀리식(브라우저↔백엔드 직결)→BFF/SSR(브라우저↔Next↔백엔드)로 바뀌며 중간 Next 계층이 Set-Cookie를 가로챈 것.
- **해결(모델API·로그인체크 서버사이드 유지, 클라이언트 fetch·백엔드 변경 없음)**:
  1. `policy/page.tsx`(SSR) — 응답 `res.headers.getSetCookie()`에서 `_AUTH_INFO_TOKEN_` **값만 읽어**(RSC는 읽기 가능) PolicyForm에 `authToken` prop 전달. 겸해서 policy 호출에 `cookie: store.toString()`로 요청 쿠키 upstream 자동 첨부.
  2. `policy-form.tsx` — `authToken`을 form POST hidden 필드 `_AUTH_INFO_TOKEN_`로 실어 `/page/join/form` 전송.
  3. `middleware.ts` — `/page/join/form` POST 가로챌 때 `_AUTH_INFO_TOKEN_`을 **진짜 브라우저 쿠키**(httpOnly, path=/, 30분)로 set + 백엔드로 보내는 `policyInfo`에서는 제외.
  → 이후 `form/page.tsx`(SSR)가 `store.toString()`(그 쿠키 포함)을 upstream 전달 → 백엔드 `form()` 인증 통과.
- **일반 원칙 확립**: SSR fetch로 유실되는 "백엔드 발급 쿠키"는, 쿠키 쓰기 가능한 계층(middleware/Route Handler/Server Action)에서 복원해야 한다. 브라우저가 Route Handler(`/api/auth`,`/api/legacy`) 경유로 호출하면 그 핸들러가 `getSetCookie()`를 relay하므로 자동(로그인이 되는 이유).
- tsc: 변경 파일(policy/page, policy-form, middleware) 에러 0.

## 17. (2026-08-02) 회원가입 none-auth 근본원인 — 미들웨어 Edge 바디읽기 + 브리지쿠키 one-time 삭제
- **증상**: 본인인증 성공 후 policy에서 authInfo 빈값 → `none-auth`. 프론트 로그 `param={"authInfo":"",...}` 전부 빈값.
- **원인 2겹**:
  1. `join-auth.tsx`가 `/page/join/policy`로 form POST → 미들웨어(Edge)가 바디를 읽어 브리지 쿠키 생성하는 구조인데 Edge 바디읽기 불안정. → **POST 수신을 Node Route Handler로 이전**: `app/api/join/policy-bridge/route.ts`(`runtime="nodejs"`, formData 안정 read), `join-auth.tsx` action=`/api/join/policy-bridge`.
  2. **진짜 근본원인**: `middleware.ts`의 "브리지 쿠키 one-time 삭제" 블록이 GET `/page/join/policy`에서 `hpw_join_policy`를 응답 `maxAge=0` 삭제 → **Next가 이 삭제를 같은 요청 RSC `cookies()` 뷰에 반영** → RSC가 읽기도 전에 비워짐(구 flow도 동일하게 당함). → **해당 블록 제거**(브리지 쿠키는 짧은 maxAge/덮어쓰기로 만료).
- 검증: 프론트 로그 `[JOIN-POLICY-BRIDGE] authInfoLen=896` + policy `landing=true,type=policy` 확인.

## 18. (2026-08-02) 회원가입 form→optional 파라미터 전송 + 프리필 연결
- **form 프리필**: `form/page.tsx`가 백엔드 `form()` 응답(`RSLT_NAME/RSLT_BIRTHDAY/TEL_NO` + 컨텍스트 `encMnm/telNo/telComCd/reqPath·reqChnl·reqPage/isUnder14/joinInfoObj`)을 `JoinInfoForm`에 전달. 이름/생년월일/휴대폰 표시(하드코딩 "본인인증 후 표시" 제거).
- **form→optional 전송**: 기존 `router.push(id)`(id만) → **레거시 hidden 필드 1:1 전부 POST**(userId/pwd/postInput/homeRoadNmAddr1·2/mailId/telNo/telComCd/encMnm/reqPath·reqChnl·reqPage/regPath=PC/hpAuthYn=Y/checkIdResult/mkt*/juniorYn/보호자필드) → `app/api/join/optional-form-bridge/route.ts`(신규 Node) → `hpw_join_optional_form` 쿠키 → `optional-form/page.tsx`가 `POST /api/join/optional-form`(백엔드 encMnm 무결성 검증). 최종 joinProc는 범위 외.

## 19. (2026-08-02) 회원가입 데이터컨트롤 레거시 정합 (#2 중복확인 · #3 주소 · #4 인증)
- **레거시 대조 결론**: 기존 form은 입력UI+목업검증만, 실 데이터컨트롤/제출 누락이었음. form.jsp 기준 이식.
- **#2 아이디/이메일 중복확인 실 API**: `join-info-form.tsx`가 가짜 `TAKEN_IDS`/무조건ok 제거 → `GET /api/join/check-id`(`result.onlnIdUsePossYn`)·`check-email`(`useFlag`) 실호출. 프록시 `app/api/legacy/[...path]` 허용목록에 `api/join/check-id`·`api/join/check-email` 추가(백엔드 `JoinResource @RequestMapping /api/join`).
- **#3 주소검색(키리스 Daum, 임베드)**: 레거시는 `postcode.v2.js`+`findZipcode.js`의 **키 없는 Daum 임베드**(appkey/confmKey 없음). 프론트도 `new window.daum.Postcode({oncomplete,onresize,width,height}).embed(el)` 동적로드로 포팅(레거시 `findZipcodeNewIframe` 동일 인라인 iframe 방식 — `#findZipCodeArea` 대응 컨테이너 div ref). zonecode→postInput, 도로명 참고항목 조합→homeRoadNmAddr1, 상세주소→homeRoadNmAddr2. 가짜 ADDRESS_BOOK 제거.
  - **CSP 원인·해결(중요)**: 최초 미동작 원인 = `next.config.mjs` CSP가 Daum 도메인 차단(`script-src`에 `t1.kakaocdn.net` 없음, `frame-src 'self'`). → 레거시 `header-ga4.jsp` CSP 미러: `script-src` += `https://t1.kakaocdn.net`, `frame-src` += `https://postcode.map.daum.net https://postcode.map.kakao.com`. **CSP는 응답헤더라 dev 서버 재시작 필요.**
- **#4 인증 정합(정정 2026-08-02)**: 처음엔 "아이핀 폐지(A안)"로 이해해 진입 아이핀을 제거했으나, 사용자 재확인 결과 요구는 **"진입은 휴대폰+아이핀 2종 유지, 보호자 인증만 휴대폰 팝업"**이었음 → **진입(`join/index`·`join/auth`) 아이핀 복원**. 보호자만 휴대폰 단일.
  - 14세미만(`isUnder14`) 보호자 동의 블록(form) → `useKcbCert(reqPage='parent')` 팝업(진입 휴대폰과 동일 KCB 모듈) → postMessage(`reqPage==='parent' && pFlag==='Y'`) → `P_authYn=Y/P_authInfo/P_userName(pInfo)/ptorFamyRelCd=90`. 미인증 제출 차단.
  - **아이핀 회원 휴대폰 SMS 점유인증(구현 완료)**: form에서 `TEL_NO` 없으면(=아이핀 회원) 통신사 select + 번호입력 + 인증요청/확인 블록 표시. 백엔드 SMS API는 이미 존재(`SmsResource @RequestMapping /api/sms`, `POST /send·/check/mobileOwnerAuthNo`, `@RequestBody` JSON). 프록시 `/api/legacy` 허용목록에 두 경로 추가. send `{mobileNo}`→`result.authNo`(암호화)+3분타이머, check `{mobileAuthNo,authNo,mobileNo,userNm=RSLT_NAME}`→성공 시 `telNo`/`encMnm(=result.data)` 로컬 state 확정. 아이핀 회원은 점유인증 완료 전 제출 차단. 제출 시 `certType=ipin|mobile` + verified telNo/encMnm/telComCd 전송.
  - 판별: `form()` 응답 `TEL_NO` 유무(휴대폰 회원=프리필 읽기전용 / 아이핀 회원=SMS 블록).
- tsc: 변경 파일 전부 에러 0.

## 20. (2026-08-02) 회원가입 최종 저장(joinProc) 파이프라인 연결 — optional→welcome
- **레거시 대조**: `optional-form.jsp` → `page.joinProc(data)` = `$.ajax POST /api/join/joinProc` (JSON: 전 단계 암호화 joinInfo + intrFildCd) → code 00 시 `welcome.spc`. 백엔드 `JoinResource.joinProc`가 `_AUTH_INFO_TOKEN_`(flagCertifiTime=Y) + `getPolicyCookie()`(join_policy_info) + 각 값 복호화(intrFildCd 제외) → `saveNewMember` → `_HPC_USER_ID_`/`_HPC_EN_MBR_NO_` 쿠키 세팅.
- **발견 문제 & 해결**:
  1. **encMnm 검증은 안전**(SHAUtil.checkEncMnm: reqChnl/reqPage/reqPath는 로그용, 해시는 `SHA512(userNm|telNo)+salt`) → 프론트가 RSLT_NAME(쿠키)·telNo만 맞추면 통과(현 구현 OK).
  2. **`join_policy_info`(동의값) 쿠키 SSR 유실** → `form/page.tsx`가 form() 응답 Set-Cookie에서 값 캡처 → `JoinInfoForm` `policyCookie` prop → 제출 hidden `__join_policy_info` → `optional-form-bridge`가 **raw Set-Cookie**(이중인코딩 방지)로 `join_policy_info` 브라우저 쿠키 복원.
  3. **암호화 joinInfo 미전달** → `optional-form/page.tsx`가 optionalForm 응답 `result.joinInfo`(암호화) 캡처 → `ReferralForm`에 전달.
  4. **최종 joinProc 미구현** → `ReferralForm`: 레거시 intrFildCd 코드(J1~J7/P1~P3/JZ, 나중에하기=JY) + `POST /api/legacy/api/join/joinProc {...joinInfo, intrFildCd}`(프록시가 쿠키·Set-Cookie relay) → code 00 시 `/page/join/welcome` push. 프록시 허용목록에 `api/join/joinProc` 추가.
  5. **welcome(완료) 페이지** → `pingModel` 대신 **SSR `GET /api/join/welcome`(쿠키 forward)** 로 joinProc 가 심은 `_HPC_USER_ID_` 쿠키에서 실제 가입 아이디를 읽어 표시(쿼리 id 폴백). 축하화면+아이디+로그인 버튼.
- ✅ **PC 회원가입 E2E 성공(2026-08-02, 로컬)** — 본인인증→약관→정보입력→가입경로→joinProc→welcome 완주, 실제 회원 저장 확인. (PC 휴대폰 인증 경로 기준)

## 21. (2026-08-02) dev 배포 라우팅 정합 — `/api/` ELB 규칙 대응
- **문제**: 프론트를 dev.happypointcard.com 에 올리니 `POST /api/join/policy-bridge 404`(+연쇄 asset 404, `$ is not defined`). **dev ELB 는 `/api/*` 를 전부 백엔드로 보냄**(그 외는 프론트). 로그인은 `/api/auth/login`(백엔드 실존)이라 OK였지만, 내가 만든 프론트 전용 `/api/` 경로들은 백엔드에 없어 404.
- **원인 2종 & 해결**:
  1. **브리지(프론트 전용 POST→쿠키→303)** `/api/join/{policy,form,optional-form}-bridge` → **`/page/join/*-bridge` 로 이동**(app/(site)/page/join/*-bridge/route.ts). `/page/`는 dev에서 프론트로 라우팅. `form.action` 3곳(join-auth/policy-form/join-info-form) 갱신. 미들웨어는 `-bridge` POST 를 **즉시 통과**(collectBody 가 바디 소비하는 것 방지 — 로컬은 /api 제외라 안전했으나 /page 이동으로 필요).
  2. **백엔드 실존 API 클라이언트 호출**(check-id/check-email/sms send·check/joinProc) → `/api/legacy/...`(프론트 프록시) 대신 **실제 경로 직접**. env 분기 헬퍼 `lib/backend-api.ts` `backendApiUrl(path)`: **배포(NODE_ENV=production)=경로 직접**(ELB→백엔드, 로그인과 동일) / **로컬(development)=`/api/legacy` 프록시**(쿠키 Domain 정리). JoinInfoForm 4곳 + ReferralForm 1곳 적용.
- SSR 페이지 호출(policy/form/optional-form/welcome page.tsx)은 `${LEGACY_BASE}/api/...` 절대경로라 dev·로컬 모두 정상(변경 불요).
- **브리지 리다이렉트 상대경로화(2026-08-03)**: dev에서 본인인증 성공 후 policy로 안 넘어감 → 콘솔 진단 결과 **CSP `form-action` 차단**. 원인: 브리지가 `NextResponse.redirect(new URL(path, req.url))`로 **절대 URL** 303 을 주는데 dev는 ELB 뒤라 `req.url`이 내부호스트(localhost 등)로 잡혀 리다이렉트 대상이 `form-action 'self'`에 불일치. → 3개 브리지 모두 **`new NextResponse(null,{status:303,headers:{Location:상대경로}})`** 로 변경(브라우저가 현재 오리진 기준 해석 → self 통과). (`[JOIN-AUTH]` 콘솔 진단으로 origin·인증·POST 정상 확인, 차단지점만 CSP였음.)
- **optional-form-bridge 502 해결(2026-08-03, dev)**: policy·form 브리지는 OK인데 optional-form-bridge에서 502. 원인 = 이 브리지가 큰 쿠키 3개(`hpw_join_optional_form` 회원정보 전체 + `_AUTH_INFO_TOKEN_` + `join_policy_info` authInfo·암호화 joinInfo·동의코드) ~6KB 를 Set-Cookie 로 내려 **nginx 기본 `proxy_buffer_size`(4k) 초과 → "upstream sent too big header"**. → **dev nginx 프론트 `location /`(→127.0.0.1:3000) 에 버퍼 상향**: `proxy_buffer_size 16k; proxy_buffers 8 16k; proxy_busy_buffers_size 32k;` → 정상. (인프라 설정. 추후 쿠키 슬림화는 선택.)
- **로그인 탭 이동 수정(2026-08-03)**: `clearable-input.tsx` X(클리어) 버튼에 `tabIndex={-1}` → 아이디 입력 후 Tab 이 X 버튼이 아니라 비밀번호로 이동.
- ✅ **dev 회원가입 E2E 완주 성공(2026-08-03)** — dev.happypointcard.com(프론트+백엔드 same-origin)에서 본인인증→약관→정보입력(주소/중복확인)→가입경로→joinProc→welcome 전 구간 완주, 실제 회원 저장 확인.
- **정리 완료**: 임시 진단 로그(`[JOIN-AUTH]`,`[ADDR]`,`[JOIN-POLICY-BRIDGE]`,`[JOIN-FORM-BRIDGE]`,`[JOIN-OPTIONAL-BRIDGE]`) 제거.
- **누락 필드 보정**(form→optional 제출): `homeRoadNmPostNo`, `labelAddress`, `ptorTelNo`/`ptorEmlAddr`(빈값) 추가.
- **옵셔널 페이지 = 레거시 정합 확정(2026-08-02)**: 레거시 `optional-form.jsp`에서 수신매체(infoRetnMda)/직업(jobCd)/결혼기념일(maryCeleDay)은 **주석처리(비활성)** → 실사용 필드는 `intrFildCd` 하나뿐. `ReferralForm`이 intrFildCd + 전단계 암호화 joinInfo 전달 + 나중에하기(JY)/가입완료로 이미 동일. **추가 이식 대상 아님(사용자 1번 선택).**
- **미결(런타임 검증 필요)**: dev 재기동 후 전 구간 E2E(휴대폰/아이핀/14세) 테스트, 임시 진단 로그(`[ADDR]`,`[JOIN-*-BRIDGE]`) 제거, welcome 페이지 표시값 확인.
